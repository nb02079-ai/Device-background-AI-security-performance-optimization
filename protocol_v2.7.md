# 실험 프로토콜 확정 문서 v2.7

**연구 주제**: 호스트 행위 기반 악성코드 탐지 모델의 탐지 성능, 학습 데이터 멤버십 노출, 특징 교란 민감도 및 추론 비용을 공동 평가하고, 진단에 기반한 선택적 개선의 효과를 검증한다.

**문서 목적**: 실험 시작(Phase 1) 전 모든 방법론적 결정을 사전에 고정하여, 결과를 보고 사후적으로 유리한 조건을 선택하는 것을 방지한다.
**상태**: v2.7 (2026-09-19) — **Phase 3 완료.** 모델 4종 구축 및 Validation 평가 완료. Phase 4 착수 가능

---

## 1. 연구 질문 및 가설

**핵심 연구 질문**: 탐지 성능이 높은 호스트 행위 기반 악성코드 탐지 모델이, 프라이버시·특징 교란 민감도·추론 비용 측면에서도 안전한가?

| 가설 | 내용 | 검증 방식 |
|---|---|---|
| H1 (확증적 가설 아님 — 1.1절 참고) | 탐지 성능(MCC·F1·AUC)이 높다고 해서 프라이버시(MIA AUC)·강건성(교란 민감도)·자원 효율성(지연시간)이 함께 보장되지는 않는다 | **모델 4종의 축별 지표를 병렬 표로 제시하고 반례 존재 여부로 판정.** 상관계수·유의성 검정을 수행하지 않는다 |
| H2 | 조작 용이 특징(6.2절)에 대한 permutation importance가 높을수록, 해당 특징 교란 시 악성 확률 감소폭이 크다 | Importance 순위와 교란 민감도 S_i의 Spearman 상관계수. **양쪽 모두 Validation에서 산출**, 분석 단위는 최종 입력 특징 컬럼(n≈12), S_i 부호 정의 포함 — 6.4절 참고 |
| H3 | 지정된 학습 조건 하에서, 모델 구조(RF/MLP) 및 튜닝 방법(기본/BO/스태킹)에 따라 파이프라인 수준의 프라이버시·강건성 차이가 관찰된다 | 4.1절 4개 모델 변형 비교. "순수 구조 효과"라는 표현은 사용하지 않음 — 특정 학습 조건 하에서 관찰된 파이프라인 간 차이로 한정해 해석한다 |
| H4 | **한 연도 코호트에서 관찰된 취약성 패턴이 다른 코호트에서도 재현된다** (DMBD 2017 vs 2024) | 축별로 개별 재현 판정 후 병기 (1.2절). 교차 데이터셋이 아닌 **코호트 간 시간적 일반화** 검증임을 명시 |
| H5 | 진단 결과에 기반한 선택적 개선은, 일괄 개선 대비 목표축 개선은 동등 이상이면서 부작용은 더 적다 | **10.4절의 이중 검정 구조**: 목표축은 비열등성 검정(Δ=selective−batch의 CI 하한 ≥ −δ), 부작용축은 우위 검정. 검정력 부족 시 "판정 불가"로 보고 |

### 1.1 H1의 위상 — 확증적 가설이 아닌 문제 제기

**H1은 통계적 가설검정의 대상이 아니다.** 본 연구의 모델은 4종(A/B/C/D)뿐이므로 축 간 상관계수를 산출하면 n=4가 되어 추론 통계로 해석할 수 없고, 시드 5회 반복을 표본 수로 환산하는 것은 동일 설정의 반복을 독립 표본으로 취급하는 유사반복(pseudo-replication)에 해당한다.

따라서 H1은 다음과 같이 취급한다.

- **제시 방식**: 모델 4종 × 4개 축(탐지 MCC / MIA AUC / 교란 flip rate / 추론 지연시간)의 지표를 **하나의 병렬 표**로 제시한다. 독자가 축 간 움직임을 직접 확인할 수 있도록 하는 것이 목적이다.
- **수행하지 않는 것**: 축 간 **상관계수를 산출하지 않으며, 유의성 검정도 수행하지 않는다.** n=4에서 계산된 상관계수는 해석 불가능하며, 제시할 경우 독자가 부당한 의미를 부여할 위험이 있다.
- **판정 기준**: H1은 "항상 함께 보장되지는 않는다"는 **부정 명제**이므로 논리적으로 **반례 하나로 충분하다.** 한 모델이라도 "탐지 성능은 상위이나 다른 축에서 하위"인 사례가 관측되면 H1이 지지된 것으로 서술한다. 반례가 없으면 "본 실험 범위에서는 반례를 관측하지 못했다"고 기록한다.
- **다중비교 보정**: 확증적 가설이 아니므로 8장 보정 대상에서 제외한다.
- **위치**: H1은 본 연구 전체를 관통하는 문제 제기이며, 실질적 검증은 H2~H5가 담당한다.

### 1.2 H4 축별 재현 판정 기준

| 축 | 재현 판정 기준 |
|---|---|
| 프라이버시(MIA) | 두 **코호트** 모두에서 **MIA AUC_adv**의 95% 신뢰구간 하한 > 0.5 (AUC_adv는 정의상 0.5 이상이므로 방향 문제가 발생하지 않는다, 5.4절) |
| 강건성(교란 민감도) | 두 **코호트** 모두에서 20% 조작 시 flip rate 증가폭의 신뢰구간이 0을 포함하지 않음 |
| (보고 지표 주의) | **PR-AUC는 기준선이 클래스 유병률에 따라 달라지므로 코호트 간 직접 비교에 사용하지 않는다.** 코호트별 클래스 구성이 크게 다르므로(11.2.1절) 특히 주의. 각 코호트 내부 비교에만 사용하고, H4 재현 판정에는 MCC를 쓴다 |
| 리소스 효율성 | 재현/미재현의 이분법 판정 대상이 아님. "무작위 기준"이라는 개념이 성립하지 않으므로, 두 **코호트**의 상대적 비용 순위만 기술적으로 비교·서술 |
| 종합 판정 | 프라이버시·강건성 2개 축의 판정 결과를 그대로 병기. "전체 재현 성공/실패"라는 단일 결론으로 뭉뚱그리지 않는다 |

---

## 1A. 연구 범위 및 관측 지점

### 1A.1 무엇을 탐지하는 모델인가

본 연구의 모델은 소프트웨어가 **실행된 이후의 호스트 행위**를 관측 대상으로 한다. 따라서 본 연구는 **침입 탐지(intrusion detection)가 아니라 실행 중 악성 행위 탐지(malicious execution detection)**에 해당하며, 공격 킬체인에서 초기 접근(Initial Access) **이후** 단계에 위치한다.

- **관측 범위 밖**: 초기 침입 벡터(취약점 익스플로잇, 음성/이메일 피싱, 자격증명 탈취, 웹 침해 등). 이들은 데이터셋에 포함되지 않는다.
- **용어 통일**: 논문 전체에서 "침입 탐지(intrusion detection)", "IDS"라는 표현을 본 연구의 모델을 지칭하는 데 사용하지 않는다. 참고 구현인 IDS-ML은 네트워크 트래픽 기반 침입탐지 시스템으로 본 연구와 문제 설정이 다르며, **코드 구조 및 최적화 파이프라인 참고 목적으로만 인용**한다(4.2.1절).
- **사유**: 두 데이터셋(CIC-MalMem-2022, DMBD 2025) 모두 "악성코드를 실행시킨 시스템의 상태/행위"를 라벨링한 것이며, 침입 시도 자체는 관측 대상이 아니다.

### 1A.2 관측 창과 위협 환경의 시간 척도

본 연구 데이터셋의 **명목 관측 창은 실행 직후 5분**이다(DMBD 2025). 단, **실제 이벤트 발생 구간은 이보다 훨씬 짧다** — `duration_s`의 중앙값은 **1.36초**, p10은 0.04초, p75는 33.08초, p90은 274.14초이며 0초 이하가 401건이다. 즉 상당수 샘플은 실행 직후 수 초 내에 관측 가능한 활동을 멈춘다. **따라서 "수 분간 관측한다"가 아니라 "최대 5분의 고정 창 안에서 관측되는 활동을 본다"가 정확한 서술이며, 논문에서 이 구분을 유지한다.** 이 분포 편중은 3.3.2절에서 발생률 정규화를 적용하지 않기로 한 직접적 근거이기도 하다.

이 관측 창은 자의적 선택이 아니라 실제 위협 환경에서 탐지가 실익을 갖는 시간 척도와 부합한다. 업계 실측 보고 기준:

| 지표 | 값 | 출처 |
|---|---|---|
| eCrime breakout time (최초 호스트→2차 호스트) | 2025년 평균 29분, 최단 27초 | CrowdStrike 2026 GTR |
| Time-to-Encrypt (실행→암호화 완료) | 10개 계열 중앙값 43분, 범위 4분~3.5시간 | Splunk 분석 |
| 초기접근 브로커→랜섬웨어 제휴사 인계 | 중앙값 22초 | Mandiant M-Trends 2026 |

→ **breakout이 발생하면 단일 지점 탐지·차단만으로는 침입을 저지할 수 없으므로, 실행 후 수 분 내 판별이 탐지의 실질적 데드라인이다.** 이는 7장 리소스 벤치마크(추론 지연시간)의 중요성에 대한 근거이기도 하다 — 탐지 여유가 분 단위인 환경에서 에이전트 자체의 지연은 직접적인 보안 위험이 된다(LIGHT-HIDS 2025의 논지와 동일).

### 1A.3 명시적 한계: 저활동 잠복형 악성코드

**본 연구가 다루는 것은 "실행 직후 관측 가능한 수준으로 활발히 활동하는 악성코드"이며, 장기 저활동 잠복형은 탐지 대상에서 사실상 제외된다.** 이는 데이터셋 설계에서 비롯된 구조적 한계로, 모델 개선으로 해소되지 않는다.

- **근거**: 2025년 전역 dwell time 중앙값은 14일이며, 첩보성 침입의 경우 122일에 달한다(Mandiant M-Trends 2026). 중간 기간(1주~6개월) 미발견 사례가 전년 대비 증가했고, 그 원인으로 **대상 환경에 이미 존재하는 도구만 사용(LOLBin), 정상 관리자 행위 모방, 포렌식 흔적 제거**가 지목된다.
- **함의**: 이런 유형은 수 분 관측 창 안에서 정상 프로세스와 구분되는 행위 신호를 거의 남기지 않는다. 따라서 본 연구 모델의 성능 수치는 **"활발한 활동을 보이는 악성코드에 대한 성능"**으로 한정 해석해야 하며, 전체 악성코드에 대한 탐지 능력으로 일반화할 수 없다.
- **보고 의무**: 논문 한계점(Limitations)에 본 항목을 명시하고, 결과 해석 시 "전체 악성코드 탐지율"이 아니라 "본 데이터셋이 포착한 활동 유형에 대한 탐지율"로 서술한다.
- **파일리스/LOLBin과의 연결**: 위 잠복 전술은 LOLBin·파일리스 기법과 상당 부분 겹친다. 특히 파일리스 악성코드는 정의상 파일 이벤트를 거의 생성하지 않으므로, 우리 특징 중 **파일 이벤트율의 판별력이 제한적일 수 있다**. Phase 4 특징중요도 분석에서 이를 확인하고, 예상과 다른 결과가 나오면 그 자체를 논의 대상으로 삼는다.
- **은밀성과 포착 난이도의 상관 (근거 보강)**: APT 악성코드의 LotL 기법 사용률은 26.26%로 일반 악성코드(9.41%)의 2배 이상이다(Barr-Smith et al., IEEE S&P 2021, 3,180만 샘플). 즉 **은밀성이 높은 공격일수록 정상 시스템 바이너리를 악용하여 본 연구가 포착하려는 행위 신호를 최소화하는 경향**이 있다. 동일 연구는 샌드박스 회피(anti-VM) 악성코드 때문에 자신들의 측정치가 하한(lower bound)이라고 명시하는데, 본 연구의 데이터셋도 샌드박스에서 수집된 이상 같은 편향을 안고 있다.

### 1A.4 명시적 한계: 비용 민감(cost-sensitive) 평가 미수행

**본 연구는 오탐(FP)과 미탐(FN)의 오분류 비용이 동등하다고 가정하며, 비용 민감 평가를 수행하지 않는다.** 이는 의식적인 범위 제한이며, 근거는 다음과 같다.

- **비용비를 정할 근거가 없다**: 악성코드 탐지에서 FN과 FP 중 어느 쪽이 더 비싼지는 조직마다 다르다. 미탐 1건은 침해 사고로 이어지는 반면, 과다한 오탐은 분석가의 경보 피로(alert fatigue)를 유발해 결국 경보 자체를 무시하게 만드는 실질적 운영 문제를 낳는다. 근거 없이 특정 비용비(예: FN이 FP의 10배)를 설정하면 그 자의적 선택이 모든 결론을 좌우하게 된다.
- **유병률 의존성**: Youden's J의 비용비는 유병률에 따라 변한다(BMC Med Res Methodol, "A note on Youden's J and its cost ratio"). 본 연구는 클래스 비율이 다를 수 있는 두 데이터셋(CIC-MalMem-2022, DMBD 2025)을 사용하므로, 고정 비용비를 두면 두 데이터셋에서 의미가 달라진다.

**보고 의무**: 논문 한계점(Limitations)에 본 항목을 명시하고, 비용 민감 임계값 설정 및 비용 가중 평가를 **향후 연구 과제**로 기술한다. 실무 적용 시에는 조직의 실제 비용 구조를 반영해 임계값과 허용 한도를 재설정해야 한다는 점을 함께 서술한다.

---

## 2. 데이터 분할 및 누수 방지 (탐지 성능 평가용 — 시간적 일반화)

### 2.1 분할 원칙 (Phase 2 재확정, 2026-09-18)

주실험 데이터셋 DMBD는 **1행 = 1 실행(`experiment`)**이므로(3.2절), 동일 실행의 복수 행이 분할에 걸치는 누수가 구조적으로 발생하지 않는다. v2.5의 비대칭 그룹 분할은 CIC-MalMem의 구조적 결함에 대응한 것이었으며, DMBD에는 적용되지 않는다.

| 구분 | 내용 |
|---|---|
| **Test** | DMBD **공식 test 분할을 그대로 보존**(24,747 실행). 재분할하지 않는다 |
| **Train / Validation** | 공식 train(40,000)을 Train 75% / Validation 25%로 재분할. 층화는 클래스 기준 |
| 그룹 단위 | `experiment` (행 = 실행이므로 추가 그룹화 불필요) |
| family 비중복 | **적용 불가** — DMBD는 malware family 라벨을 제공하지 않는다. 대신 연도 코호트가 일반화 축이 된다(11.2절) |

**한계 명시 의무**: family 라벨 부재로 "미지 family 일반화" 평가는 수행할 수 없다. v2.5까지 기본 설정으로 두었던 미지 family 탐지 평가는 **시간적 일반화(코호트 간) 평가로 대체**된다. 이 변경을 논문 방법론에 명시한다.

### 2.2 Test 고정과 시드 반복
Test는 최초 1회만 분할·고정. 5개 시드(42, 7, 123, 2024, 777)는 모델 초기화·학습·교란 탐색에만 적용하고 Test 재분할에는 적용하지 않는다.

### 2.3 전처리 누수 방지
결측치 처리·스케일링·특징선택·클래스 재표집은 Train 내부에서만 적합. 하이퍼파라미터 튜닝은 **Train 내부 3-fold 교차검증**으로 수행하고 그 결과를 최종 성능으로 사용하지 않는다. (엄밀한 의미의 nested CV는 inner/outer 이중 CV 구조를 뜻하므로, 본 설계를 nested CV로 부르지 않는다. 본 설계는 Train 내부 CV로 튜닝하고 별도의 Validation·Test로 평가하는 hold-out 구조다.)

---

## 3. 공통 특징 확정 및 데이터 가공 규칙

### 3.1 공통 특징 확정 규칙

v1.2와 동일: 원본컬럼/이벤트-상태구분/집계대상/관찰시간창/계산식 5요소 문서화 필수, 0과 결측 구분, 우측절단 프로세스 플래그, 강제통합 금지, 공통특징 2개 이하 시 H4 범위 축소.

**특징 선택의 실증적 근거 (신설)**: 3,180만 개 샘플을 분석한 Barr-Smith et al.(IEEE S&P 2021)에 따르면, Reg.exe는 악성코드가 가장 빈번하게 실행하는 LotL 바이너리 중 하나이며(VT Balanced 데이터셋 15.49%, Georgia Tech 데이터셋 42.16%), **레지스트리 변경·정찰·지속성 확보는 정상 소프트웨어 대비 악성 소프트웨어에서 유의하게 더 빈번한 실행 목적**으로 확인되었다. 반면 프록시 실행(proxied execution)은 정상·악성 양쪽에서 가장 빈번해 판별력이 낮다. 본 연구의 특징 집합(레지스트리·네트워크·프로세스 생성률 중심)은 이 관찰에 부합한다.

### 3.2 분석 단위 (Phase 2에서 재확정, 2026-09-18)

**1개 샘플 = 1회 실행(`experiment`)**으로 정의한다.

> **경위**: v2.5에서는 CIC-MalMem-2022의 정상 샘플에 실행 식별자가 없어(3.2.1절) 분석 단위를 "1 메모리 덤프(1행)"로 낮출 수밖에 없었다. 주실험 데이터셋을 DMBD로 교체(11.1절)하면서 **`experiment` 필드가 정상·악성 양쪽에 모두 존재**하므로, 본래 의도했던 실행 단위 정의를 **대칭적으로** 복원한다.

- DMBD의 집계 결과는 이미 실행당 1행이므로 추가 집계가 불필요하다
- 부트스트랩·분할·MIA member 정의의 기본 단위를 모두 `experiment`로 통일한다
- 클래스 간 비대칭 처리가 없으므로, v2.5에서 문제가 되었던 "std=0이 클래스를 판별하는" 누수가 발생하지 않는다

### 3.2.1 (사례 연구 기록) CIC-MalMem-2022의 그룹 구조 문제

아래는 주실험 데이터셋이 아닌 **사례 연구 대상**(11.3절)에 대한 Phase 1 조사 기록이며, 현재 실험 절차에는 적용되지 않는다.

| 확인 항목 | 결과 |
|---|---|
| 악성 `Filename` 구조 | `{Category}-{hash}-{dumpIndex}.raw`, 해시 길이 21~64자 가변 |
| 악성 실행 수 | 2,906개 (15 family) |
| 정상 `Filename` | 전 행 `"Benign"` — 실행 식별자 부재 |
| 정상 주기 구조 검정 | 인접 행 표준화 거리 악성 0.268 vs 정상 0.782, 위치별 변동 완전 평탄 → 세션 구조 복원 불가 |

이 구조적 비대칭은 해당 데이터셋에서 실행 단위 집계를 불가능하게 하며, 11.3절의 artifact 문제와 함께 벤치마크 타당성 논의에 포함한다.

### 3.3 (폐기) 스냅샷 집계 방식

3.2절 결정에 따라 폐기한다. 집계를 수행하지 않으므로 통계 요약(평균·최대·표준편차·기울기) 특징을 생성하지 않는다.

### 3.3.1 (폐기) 감염 시점 이중 설정

3.3절 폐기에 따라 적용 대상이 소멸했다. 아울러 Phase 1 확인 결과 본 데이터셋에는 감염 시점 라벨 자체가 존재하지 않는다.

### 3.3.2 특징 집합 확정 — DMBD (Phase 2, 2026-09-18)

**최종 특징 14개** (실행당 집계, 원시 이벤트 로그에서 산출)

| 분류 | 특징 | 비고 |
|---|---|---|
| 이벤트 카운트 (9) | `process_create`, `process_term`, `image_load`, `net_connect`, `file_create`, `reg_create_key`, `reg_write`, `reg_delete_key`, `reg_delete_value` | 공식 베이스라인과 동일 구성 |
| 시간 (1) | `duration_s` | 첫 이벤트~마지막 이벤트 |
| 다양성 (3) | `n_uniq_child`, `n_uniq_parent`, `n_uniq_cguid` | 고유 개체 수. 같은 파일 1,000회 접근과 1,000개 파일 접근을 구분 |
| 총량 (1) | `n_events` | 9개 카운트의 합 — **파생 지표이므로 교란 대상에서 제외**(6.2절 정의상 제약) |

**제외 특징 2개**
- `detonation` — 전 실행에서 항상 1(상수)
- `dur_after_detonation_s` — `duration_s`와 **100% 동일**(detonation이 항상 첫 이벤트). 완전 중복

**발생률(rate) 정규화를 적용하지 않는 결정** ⚠️

v2.5까지 "분당 발생률"로 정규화할 계획이었으나 **원시 카운트를 그대로 사용**한다.

- **사유**: DMBD의 `duration_s` 분포가 극도로 편중되어 있다 — 중앙값 **1.36초**, p10 **0.04초**, p75 33.08초, p90 274.14초, **0초 이하 401건**. 이 값으로 나누면 값이 폭발하고 0 나눗셈이 발생한다
- 모든 샘플의 **명목 관측 창이 5분으로 동일**하므로 원시 카운트가 이미 비교 가능한 양이다("고정 창 내 활동량")
- `duration_s` 자체를 별도 특징으로 유지하므로 활동 지속 정보는 소실되지 않는다
- 공식 베이스라인도 원시 카운트를 사용한다

**중복 제거 규칙**: Spearman |ρ| > 0.95인 쌍에서 컬럼 순서상 뒤의 특징을 제거한다. Train 내부에서만 판단하고 Validation/Test에는 결정된 목록을 적용한다. 제거 내역을 14장에 기록한다.

### 3.4 처리 순서 (누수 방지)

아래 순서를 엄격히 준수한다. 특히 **분할(3단계)이 스케일링·불균형처리(4~6단계)보다 반드시 선행**해야 한다.

```
1. 집계된 실행 단위 테이블 로드 (64,747 실행 × 14 특징)
2. 제외 특징 2개 제거 (detonation, dur_after_detonation_s)
3. 공식 train/test 분할 보존 + 공식 train을 Train/Validation으로 재분할 (11.2.1절)
4. Train에서만 중복 특징 판단·제거 (3.3.2절), 제거 목록을 Val/Test에 적용
5. Train에서만 스케일러 fit (MLP용), Val/Test는 transform만
6. Train에서만 클래스 불균형 처리 — 소수 클래스 39.0% < 40%이므로 Class Weight 적용 (4.3절)
7. Train 내부 3-fold CV로 하이퍼파라미터 탐색 (2.3절)
```

- **금지 사항**: 전체 데이터셋에 대한 min-max 정규화 후 분할, 전체 데이터 기준 이상치 제거, 분할 전 재표집(SMOTE 등). 이는 각각 test snooping 및 selective snooping에 해당한다(Arp et al. 2022, P3).
- **참고**: 본 연구가 참고한 IDS-ML 구현은 전체 `df`에 정규화를 적용한 뒤 분할하는 순서를 취하고 있어 이 원칙에 위배된다. 4.2.1절의 상이점 목록에 이 항목을 추가 기록한다.

---

## 4. 모델 구성, 하이퍼파라미터, 탐색 범위

### 4.1 모델 4종

| 구분 | 모델 | 역할 |
|---|---|---|
| A | RandomForest (기본) | 주 배포 후보, 기준점 |
| B | RandomForest + 베이지안 최적화 (스태킹 없음) | 튜닝 효과만 분리 |
| C | RandomForest + 베이지안 최적화 + 스태킹 | 튜닝+구조결합 효과 |
| D | MLP | 모델 구조 대조군 |

### 4.2 탐색 범위 부록표 (Phase 1 데이터 규모 확인 후 최종 수치 확정, 아래는 기본값)

| 항목 | 내용 |
|---|---|
| RF (A) | **본 연구 고정 기준값(protocol-fixed baseline): n_estimators = 300**, 그 외는 scikit-learn 1.9.1 기본값(max_features='sqrt', max_depth=None 등) 사용. ⚠️ scikit-learn 1.9.1의 n_estimators 라이브러리 기본값은 100이며, 300은 **본 연구가 지정한 값**이다 — 모델 A는 전체 비교의 기준점이므로 트리 수 부족에 따른 분산을 줄이기 위해 라이브러리 기본값보다 큰 값을 고정했고, 13A절 복잡도 스윕의 최대 단계(300)와 일치시켰다. 탐색 범위는 아래 BO 항목의 공간을 따름 |
| MLP (D) | **튜닝하지 않고 scikit-learn 기본값 `hidden_layer_sizes=(100,)` 사용** (Phase 3 확정). 모델 A도 미튜닝이므로 A↔D 구조 비교의 대칭성을 확보하기 위함이며, 임의 선택을 배제하고 라이브러리 기본값을 근거로 삼는다. 조기종료: Validation loss 10 epoch 정체 시 중단 / 최대 200 epoch. (참고: 당초 유닛 범위 [16, 128] / 조기종료: Validation loss 10 epoch 정체 시 중단 / 최대 200 epoch |
| BO (B, C) | **알고리즘: GP 기반 베이지안 최적화 (Matérn 2.5 커널 + Expected Improvement)** — Phase 3에서 `hyperopt` 미설치·네트워크 차단으로 TPE 사용 불가하여 대체. 탐색 공간은 불변. 초기 무작위 10회 + BO 20회 = 총 30회 | / 목적함수: **Train 내부 3-fold CV의 MCC** (Test 미사용, 8장 주지표와 일치) / 시행 30회 / 내부 그룹분할: Train 내 3-fold (family 비중복 유지) |
| BO 탐색공간 (RF 계열) | n_estimators [10, 200] / max_depth [5, 50] / **max_features [1, 12]** (Phase 3 확정: 실제 특징 수 12에 맞춰 상한 조정) / min_samples_split [2, 11] / min_samples_leaf [1, 11] / criterion {gini, entropy} — IDS-ML(Yang et al.) 검증 범위를 채택. Phase 1에서 특징 수 확인 후 max_features 상한만 실제 특징 수에 맞게 조정 |
| 스태킹 (C) | 베이스모델: RF 3종(다른 시드, B의 BO 최적 파라미터 사용) + **HistGradientBoosting 1종**(Phase 3에서 연산 시간 문제로 GradientBoosting에서 대체, 동일 알고리즘 계열의 고속 구현) / 메타모델: 로지스틱회귀 / OOF 분할: Train 내 5-fold |
| 개선후보-정규화+이산화 | 정규화 강도 3단계, 출력 이산화 소수점 1~2자리 |
| 개선후보-교란학습 | 6장 flip 성공 샘플을 원레이블 유지 증강, 비율 [10%, 50%] 탐색 |
| 개선후보-경량화 | RF 트리 수 [20, 100] / MLP 유닛 축소비율 [30%, 70%] |
| 일괄개선 비교군 | 순서: 경량화→정규화+이산화→교란학습 순 고정 적용 / 선택적 개선과 총 탐색예산 동일하게 맞춤 |

> 탐색 도중에는 이 범위를 넓히지 않는다.

### 4.2.1 선행 구현(IDS-ML) 대조 결과 및 의도적 상이점 (2026-09-15 기록)

IDS-ML 저장소(Western OC2 Lab, Yang et al.)의 세 노트북을 코드 수준에서 분석한 결과, 본 프로토콜과 **의도적으로 다르게 설계한 지점** 3가지를 확인하였다. 논문 방법론 서술 시 "선행 연구의 일반적 관행 대비 개선점"으로 명시한다.

| 항목 | IDS-ML 구현 | 본 프로토콜 | 상이 사유 |
|---|---|---|---|
| 스태킹 메타모델 학습 데이터 | 베이스 모델이 **자신의 학습 데이터(X_train)를 그대로 예측한 결과** 사용 (`Tree-based_IDS_GlobeCom19` L320-327) | OOF(Out-Of-Fold) 5-fold 예측 사용 | in-sample 예측은 베이스 모델의 과적합된 출력이므로, 메타모델이 "베이스 모델은 항상 옳다"는 비현실적 패턴을 학습하게 됨 (데이터 누수) |
| BO 목적함수 | **Test set 점수**를 최대화 (`MTH_IDS_IoTJ` L453-455) | Train 내부 3-fold CV의 MCC | Test로 튜닝하면 Test가 더 이상 미관측 데이터가 아니게 되어 최종 성능이 과대평가됨 |
| 데이터 분할 | 단순 층화 무작위 2분할 (`train_test_split(stratify=y)`) | 공식 분할 보존 + 코호트별 분리 보고 (2.1·11.2.1절) | 무작위 분할은 시간적 일반화를 평가하지 못하며, 학습보다 미래 시점 샘플로 평가하는 조건을 만들지 못함 |
| 데이터 분할 (CIC-MalMem-2022 선행 연구) | 58,596행에 단순 무작위 분할 — 같은 실행의 10개 덤프가 train/test에 동시 존재 | (해당 데이터셋은 사례연구로만 사용, 11.3절) | 본 연구는 이 데이터셋으로 모델을 학습하지 않으므로 직접 비교 대상은 아니나, 11.3절 벤치마크 타당성 논의에 함께 기록한다 |
| 전처리 순서 | 전체 `df`에 min-max 정규화 적용 후 분할 (`Tree-based_IDS_GlobeCom19` 1.2.2절 → 1.2.3절 순서) | 분할 후 Train에서만 fit, Val/Test는 transform만 (3.4절) | 전체 데이터로 정규화하면 Test의 최대·최소값 정보가 Train에 유입됨 (test snooping) |

**채택한 부분**: BO-TPE 알고리즘(hyperopt) 및 RF 하이퍼파라미터 탐색 공간(4.2절 반영). 스태킹 베이스 모델 구성(DT/RF/ET/XGBoost 4종)은 본 프로토콜(RF 3종+GB 1종)보다 다양성이 높아 Phase 3에서 재검토 여지 있음 — 단 변경 시 14장에 기록.

### 4.3 클래스 불균형 처리 — 우선순위 체인 (택일)

> **v2.2까지의 "Class Weight → Under-sampling → Over-sampling 순"이라는 표현은 모호했다.** 세 방법을 순차 누적 적용하는 것으로 읽힐 수 있었으나, 실제 의도는 **택일**이다. Under-sampling과 Over-sampling을 동시에 적용하면 서로 상쇄되거나 왜곡된 분포가 생성되므로 누적 적용은 수행하지 않는다.

세 방법은 **독립적인 후보이며 조건에 따라 하나만 선택**한다.

| 순위 | 방법 | 적용 / 전환 조건 |
|---|---|---|
| 1 | Class Weight (`class_weight='balanced'` 등) | 소수 클래스 비율 40% 미만일 때 기본 적용 |
| 2 | Under-sampling (다수 클래스 축소) | 1순위 적용 후에도 Validation 소수 클래스 재현율 < 0.5 |
| 3 | Over-sampling (SMOTE 등) | 소수 클래스 표본이 30개 미만이라 1·2순위가 부적절한 경우 |
| — | **처리하지 않음** | 소수 클래스 비율이 40% 이상이면 불균형 처리를 적용하지 않는다 |

- 모든 처리는 **Train 내부에서만** 수행한다(2.3절, 3.4절). 분할 전 재표집은 금지된다.
- 어떤 방법이 선택되었는지(또는 미적용인지)를 데이터셋별·모델별로 14장 변경이력에 기록한다.
- **Phase 3 정정 (2026-09-19)**: Phase 2에서 전체 데이터 기준(소수 39.0%)으로 Class Weight 적용을 판정했으나 **이는 오류였다.** 클래스 가중은 학습 시 적용되므로 **Train 분포로 판정해야 한다.**
  - 공식 train 40,000건: 정상 20,000 / 악성 20,000 = **정확히 50:50**
  - 소수 클래스 비율 50% ≥ 40% → **불균형 처리 미적용** 확정
  - 참고: 공식 test 24,747건은 악성 78.8%로 Train과 사전확률이 크게 다르다(11.2.2절)
- 본 절은 **오분류 비용 가중이 아니라 클래스 불균형 보정**이다(8장 등가중 가정 선언, 1A.4절 참고).

---

## 5. 멤버십 추론 공격(MIA) 프로토콜 — 독립 분할 설계

> "MIA는 정의상 Test를 사용하지 않는다"는 v1.2의 표현은 부정확했다. Member/Non-member 구분은 학습 포함 여부일 뿐이며, Test를 non-member로 쓰지 않는 것은 연구 설계상의 선택이다. 이번 버전은 그 선택을 명시적으로 재설계한다.

### 5.0 Target model의 정의 (신설)

MIA의 공격 대상(target model)은 **4.1절의 모델 A/B/C/D 각각을, 5.1절(또는 5.2절)의 MIA 전용 분할로 재학습한 인스턴스**이다.

- **하이퍼파라미터**: Phase 3에서 확정된 값을 그대로 사용하며, MIA용으로 재탐색하지 않는다. (재탐색하면 MIA 전용 분할에 대한 튜닝이 되어 공격 결과가 왜곡된다)
- **Phase 3 모델과의 관계**: 알고리즘·설정은 동일하나 **학습 데이터가 다른 별개 인스턴스**이다. Phase 3의 탐지 성능표에 등장하는 모델과 동일한 객체가 아니므로, 보고 시 이를 명시하고 두 결과를 직접 결부하지 않는다.
- **H3과의 연결**: 모델 4종 각각을 공격하므로 **모델 구조·튜닝 방법에 따른 MIA 취약성 차이**를 관찰할 수 있다. 이것이 H3 검증에 필요한 구성이다.
- **보고 형식**: MIA 결과표는 모델별(A/B/C/D)로 제시하며, 표 각주에 "MIA 전용 분할로 재학습한 인스턴스 대상"임을 명시한다.

### 5.1 기본안 (옵션 A)
2장의 분할과 별도로, MIA만을 위한 독립적인 member/non-member 셋을 구성한다. (아래 조건은 family 구조가 있는 데이터셋 전용이며, **DMBD에는 적용되지 않는다** — 5.2절로 전환)
- 적용 조건: family 내 서로 다른 개별 샘플(변종·실행 인스턴스)이 2개 이상 존재하는 family가 전체 family의 30% 이상일 때 적용 가능
  - **Phase 2 재확인 (2026-09-18)**: 주실험 데이터셋이 DMBD로 교체되면서 **family 구조가 존재하지 않는다.** 따라서 family 기반 member/non-member 매칭(옵션 A)은 적용 불가하며, **5.2절 옵션 B로 자동 전환**한다. 옵션 B 적용 시 의무인 confound 명시 문구를 사용하되, 본 데이터셋에서는 family가 아니라 **연도 코호트**가 잠재 교란 요인이므로 member/non-member의 **연도 분포를 매칭**하여 이를 통제한다.
  - **5.3절 shadow-model 조건**: 클래스별 고유 실행 수가 악성 39,498 / 정상 25,249로 모두 100 이상이므로 **1순위(Shadow-model 기반 MIA) 적용 확정**
- 구성 방법: 그런 family에서 한 개별 샘플은 target model 학습(Train)에 포함, 같은 family의 다른 개별 샘플은 학습에서 제외해 non-member 후보로 사용 → family 분포가 실제로 일치하면서 차이가 "학습 포함 여부"로만 귀결됨
- 역할별 데이터 3분리 (재사용 금지):
  - 공격 학습·임계값 보정: member/non-member 각 50% 무작위 추출
  - 개선안(프라이버시 방어) 선택 근거: 나머지 50%의 절반
  - 최종 감사(Phase 6 전용): 남은 절반. ID는 Phase 4 시점에 고정, 이후 어떤 단계에서도 재열람하지 않는다

### 5.2 대체안 (옵션 B, 조건부 자동 전환)
옵션 A 적용조건(family 구조 존재)을 충족하지 못하면 자동 전환. **주실험 데이터셋 DMBD는 family 라벨이 없으므로 본 옵션이 적용된다.**
- Member = Train, Non-member = Validation (2장 분할 그대로)
- 이 경우 **"교란(confound)이 통제되지 않은 MIA 결과"**임을 명시하고, "실제 멤버십 신호를 보장한다"는 표현은 사용하지 않는다.
- **DMBD 적용 시 교란 통제**: family 대신 **연도(year)**가 주요 잠재 교란 요인이므로, member/non-member의 **연도 분포를 매칭**하여 구성한다. 연도를 매칭해도 통제되지 않는 잠재 요인이 남을 수 있음을 한계로 기술한다.

### 5.3 공격 방법 우선순위 체인

| 순위 | 방법 | 적용 조건 |
|---|---|---|
| 1순위 | Shadow-model 기반 MIA | 클래스별 고유 그룹 수 ≥ 100 |
| 2순위 | Confidence-threshold 기반 MIA | 1순위 미충족 시 자동 전환 |

- 축 제외 없음: 공격 성능이 낮게 나와도(AUC≈0.5) 축 자체를 제외하지 않는다. 보고 문구: "사전 정의된 공격 조건 하에서 멤버십 신호가 검출되지 않았다. 이는 프라이버시가 보장됨을 의미하지 않는다."
- 평가지표: Accuracy, Precision, Recall, F1, ROC-AUC, TPR@1%FPR

### 5.4 방향 보정 지표 AUC_adv (신설)

> **문제**: MIA AUC가 0.5보다 낮다고 해서 더 안전한 것이 아니다. 예컨대 AUC = 0.40이면 공격자는 판정 방향을 뒤집기만 해도 1 − 0.40 = 0.60의 성능을 얻는다. 즉 **0.5로부터의 거리**가 실질적 공격 가능성이다.

본 연구의 프라이버시 축 지표는 아래 **방향 보정 지표**로 통일한다.

```
AUC_adv = max(AUC, 1 − AUC)      (항상 0.5 ≤ AUC_adv ≤ 1)
```

- 예: AUC 0.71 → AUC_adv 0.71 / AUC 0.40 → AUC_adv **0.60**
- **적용 범위**: 8장 개선 성공 판정, 10.1절 개선 적용 조건, 10.4절 H5 비열등성 검정, 1.2절 H4 재현 판정에 모두 **AUC_adv를 사용**한다.
- **원시 AUC 병기**: 결과표에는 원시 AUC와 AUC_adv를 함께 제시하고, 원시 AUC가 0.5 미만으로 관측된 사례가 있으면 그 사실을 명시한다(공격자 모델이 체계적으로 역방향 신호를 학습했다는 뜻이므로 그 자체가 보고 가치가 있다).

---

## 6. 특징 교란 민감도 실험

> 용어 통일: 이 실험 전체를 "특징 교란 민감도 실험"으로 부르며, "회피 공격", "회피 성공률"은 문서 전체에서 사용하지 않는다. 결과 지표명은 "특징 공간 제약을 만족하는 교란에 대한 예측 전환율(flip rate)"로 통일.

### 6.1 측정 방식
- 분류 임계값 용도 분리: flip rate는 항상 고정 임계값 0.5 기준 (일관된 스트레스테스트 기준점 확보). 8장의 Validation 기반 가변 임계값은 Phase 3 탐지성능 지표 산출에만 적용하며 본 실험에는 적용하지 않는다.
- 이중 지표: (1) 고정 임계값 0.5 기준 flip rate, (2) 악성 확률 감소폭(연속값)
- 조작 단계: 5/10/15/20/30% 감소
- 분모: 원래 모델이 정확히 악성으로 분류한 표본 수만 포함 (원래 미탐 표본 제외)

### 6.2 유효성 기준 — 검증 가능/불가능 분리

본 실험 환경에는 악성코드 실행 샌드박스가 없으므로 "실제 악성 기능 유지 여부"는 검증 불가능하다. 유효성 판정은 검증 가능한 조건만으로 한정한다.

**검증 가능 (유효성 판정에 사용)**: 특징값 비음수 제약, 데이터 관찰 범위(Train 내 min/max) 초과 여부, 특징 간 정의상 제약 위반 여부

**검증 불가 (가정으로만 명시)**: 실제 악성 기능의 유지 여부 — 아래 난이도표의 "기능유지" 열은 검증된 사실이 아니라 전문가 판단에 근거한 가정임을 각주로 명시

### 6.3 조작 난이도 분류 — DMBD 특징 기준 (Phase 2 재작성, 2026-09-18)

> **재작성 사유**: v2.5까지의 난이도표는 "분당 네트워크 연결 수" 등 가상의 발생률 특징을 전제로 작성되었다. 주실험 데이터셋 확정(11.1절)에 따라 **실제 14개 특징을 대상으로 전면 재작성**한다. 6.2절의 판정 기준(①정상 기능 유지 ②권한 불필요 ③반복 조작 가능, **3개 중 2개 이상 → 쉬움**)은 변경하지 않는다.

| 특징 | ①기능유지* | ②권한불필요 | ③반복가능 | 분류 | 판단 근거 |
|---|---|---|---|---|---|
| `net_connect` | O | O | O | **쉬움** | C2 통신 주기를 늦추거나 연결을 분산해도 통신 자체는 유지됨 |
| `file_create` | O | O | O | **쉬움** | 파일 생성 시점 지연·분산 가능. 드롭 자체는 유지 |
| `reg_write` | O | O** | O | **쉬움** | HKCU 값 쓰기는 사용자 권한으로 가능 |
| `reg_create_key` | O | O** | O | **쉬움** | HKCU Run 키 등 사용자 권한으로 생성 가능 |
| `reg_delete_key` | O | O | O | **쉬움** | 흔적 삭제는 선택적 동작이므로 생략·지연해도 악성 기능 유지 |
| `reg_delete_value` | O | O | O | **쉬움** | 위와 동일 |
| `n_uniq_child` | O | O | O | **쉬움** | 생성 파일·프로세스 이름을 다양화/단일화하는 것은 기능과 무관 |
| `n_uniq_cguid` | O | O | O | **쉬움** | 위와 동일 |
| `duration_s` | O | O | O | **쉬움** | sleep 삽입으로 연장 가능 (단축은 제약이 있어 **증가 방향만** 유효) |
| `image_load` | X | O | X | **어려움** | DLL 로드는 악성 기능 수행에 직결. 로드를 줄이면 기능이 손상됨 |
| `process_create` | X | O | X | **어려움** | 프로세스 생성은 페이로드 실행의 핵심 |
| `process_term` | X | O | X | **어려움** | 생성 수에 종속되어 독립 조작 불가 |
| `n_uniq_parent` | X | O | X | **어려움** | 프로세스 트리 구조에 종속 |
| `n_events` | — | — | — | **교란 대상 제외** | 9개 카운트의 합인 **파생 지표**. 독립 조작이 정의상 불가능하므로 6.2절 "특징 간 정의상 제약"에 해당 |

\*①기능유지 열은 실행 기반으로 검증된 값이 아니라 전문가 판단에 근거한 **사전 가정**이다(6.2절). 본 연구 환경에는 악성코드 실행 샌드박스가 없어 검증 불가하다.

\*\*레지스트리의 "권한 불필요" 판정은 HKCU(현재 사용자) 키를 기준으로 한다. HKLM(공통) 키는 관리자 권한을 요구한다(Dieterich et al., ICISSP 2023). 악성코드가 권한 제약을 피해 HKCU를 활용하는 사례는 Cerber 랜섬웨어 분석에서 확인된다(Barr-Smith et al., IEEE S&P 2021).

**교란 대상**: "쉬움" 9개 중 중복 제거로 잔존한 **7개**(`net_connect`, `file_create`, `reg_write`, `reg_create_key`, `reg_delete_key`, `reg_delete_value`, `duration_s`). "어려움" 4개와 파생 지표 `n_events`는 제외한다. (6.4절 참고)

**정합성 제약 (6.2절 검증 가능 조건에 추가)**: 교란 후 `n_events`를 9개 카운트의 합으로 **재계산**한다. 9개 카운트를 바꿨는데 `n_events`가 그대로이면 정의상 모순이므로 무효 변형으로 처리한다.

### 6.4 특징 중요도 산출 및 H2 검정 기반 (신설)

H2는 **특징 중요도**와 **교란 반응**의 관계를 묻는 가설이므로, 두 값이 동일한 데이터·동일한 특징 집합 위에서 산출되어야 상관 분석이 성립한다. 아래를 사전 고정한다.

| 항목 | 설정 | 사유 |
|---|---|---|
| 산출 데이터 | **Validation** | Train에서 산출하면 과적합된 중요도가 나온다(모델이 학습 데이터를 암기했다면 모든 특징이 중요해 보인다). Test는 9장 보호 원칙상 사용 불가 |
| scoring metric | **MCC** | 8장 주지표와 일치시켜 프로토콜 전체의 일관성 유지 |
| n_repeats | **10** | scikit-learn 기본값은 5이나, 본 연구는 표본이 축소된 상태(3.2절)라 변동이 크므로 반복을 늘린다 |
| 보고 형식 | 특징별 평균 ± 표준편차 | 단일 값만 제시하면 불안정성이 드러나지 않는다 |

**H2의 분석 단위 (필수 고정)** ⚠️

H2의 Spearman 상관을 계산할 때 **분석 단위는 "최종 모델 입력 특징 컬럼 1개"**로 정의한다. 기저 행위 종류(네트워크·파일·레지스트리 등) 단위가 아니다.

- **사유**: 3.3.2절 확정 특징 14개 중 교란 대상은 "쉬움" 9개였으나, **Phase 3의 중복 제거로 `n_uniq_child`·`n_uniq_cguid`가 탈락하여 실제 n = 7**이 되었다.
  - 제거 근거: Train에서 `n_events`와 `n_uniq_cguid`의 Spearman ρ = **1.000**(실측 99.998% 동일값), `n_uniq_child`와는 ρ = 0.983. 결정론적 규칙(컬럼 순서상 뒤 제거)에 따라 둘이 탈락하고 `n_events`가 잔존했다.
  - ⚠️ **탈락한 둘은 "쉬움"(교란 대상)이었고 잔존한 `n_events`는 교란 제외 항목**이므로, 규칙의 결과가 H2의 검정력을 약화시켰다. **그럼에도 규칙을 사후 변경하지 않는다** — 결과를 보고 규칙을 뒤집는 것은 본 프로토콜이 금지하는 사후 선택에 해당한다. 이 사실을 논문 한계점에 명시한다.
  - **최종 교란 대상 7개**: `net_connect`, `file_create`, `reg_write`, `reg_create_key`, `reg_delete_key`, `reg_delete_value`, `duration_s`
- **대상 범위**: 6.3절에서 "쉬움"으로 분류된 9개 특징만 포함한다. "어려움" 4개와 파생 지표 `n_events`는 교란 대상이 아니므로 제외한다.
- **한계 명시**: n = 7은 상관 분석으로는 매우 작다. 논문에 표본 수를 명시하고, 결과를 강한 증거가 아니라 **경향 관찰**로 서술한다. 3.3.2절 중복 제거로 특징이 줄어들면 실제 n은 더 작아지므로 최종 n을 반드시 보고하고, 결과를 **경향 관찰**로만 서술한다.

**교란 민감도(기울기)의 부호 정의 (필수 고정)**

"기울기"라는 표현만으로는 부호가 모호하므로 아래로 고정한다.

```
S_i = − Δp_malicious / Δreduction
```

즉 **특징 i를 감소시켰을 때 악성 확률이 많이 떨어질수록 S_i가 커진다.** 따라서 S_i가 클수록 교란 민감도가 높다는 방향으로 통일되며, permutation importance(높을수록 중요)와 **같은 방향**이 되어 H2의 정방향 상관 예측과 정합한다.

**6장 교란 실험과의 정합**: 6.1절의 교란 민감도 측정 역시 **Validation의 "원래 모델이 정확히 악성으로 분류한 표본"**에 대해 수행한다(6.1절 분모 정의와 동일). 따라서 H2의 Spearman 상관은 **동일한 Validation 집합에서 산출된 두 값** 사이의 관계가 된다.

**Test 사용 금지**: 특징 중요도 산출과 교란 민감도 실험 어느 쪽에도 Test를 사용하지 않는다. Test는 Phase 6 최종 검증 전용이다(9장).

**MDI를 쓰지 않는 이유**: RandomForest가 기본 제공하는 `feature_importances_`(불순도 감소 기반, MDI)는 카디널리티가 높은 특징을 과대평가하고 **학습 데이터 기준**이라 과적합의 영향을 받는다. 본 연구는 순열 중요도(permutation importance)를 사용하며, MDI는 참고용으로도 보고하지 않는다.

**모델 간 공정 비교**: 순열 중요도는 모델 종류에 무관하게 동일한 절차로 적용되므로, 모델 4종(A/B/C/D)에 같은 방식으로 산출하여 비교한다. 트리 전용 기법(TreeSHAP 등)은 모델 간 비교 가능성을 해치므로 본 프로토콜에서는 사용하지 않는다.

---

## 7. 리소스 벤치마크 조건

v1.2와 동일: 전처리/추론 비용 분리, events-per-batch = 집계된 특징벡터 수, tracemalloc + psutil RSS 병행, 워밍업·단일스레드·모델적재 고정, 10/100/1,000 events/min 3단계, 20회 반복.

---

## 8. 통계 처리

- 비교 방법: 모델·개선 전후 비교는 동일 평가 그룹에 대한 대응 비교로 수행하고, **지표 차이 자체의 95% 신뢰구간**을 산출한다. **개별 신뢰구간의 중첩 여부는 유의성 판정에 사용하지 않는다.**
- **주평가지표: MCC (Matthews Correlation Coefficient)** — 탐지 성능의 의사결정 지표로 MCC를 지정한다.
  - **사유**: F1은 혼동행렬의 진음성(TN)을 계산에 포함하지 않는다(F1 = 2TP/(2TP+FP+FN)). 본 연구는 오탐률을 핵심 관심사로 다루므로(10.2절 허용 한도), 네 칸 모두에 비례하는 MCC가 의사결정 지표로 더 적합하다(Chicco & Jurman, *BMC Genomics* 2020).
  - **반론 인지**: MCC 역시 클래스 불균형에 완전히 강건하지는 않다는 지적이 있다(Holzmann & Klar). 논문 서술 시 이 반론을 함께 언급한다.
- **지표·임계값의 성격 규정 (비용 관점)**: 아래 세 요소는 성격이 서로 다르므로 구분해서 서술한다. 어느 것도 "실제 FP 비용 = FN 비용"임을 입증하거나 전제하지 않는다.

| 요소 | 정확한 성격 | 하지 말아야 할 서술 |
|---|---|---|
| 주지표 **MCC** | 혼동행렬 네 칸의 정보를 **균형적으로 반영**하는 요약 지표 | "FP/FN 비용이 같다는 증거" |
| **Youden's J** | 민감도와 특이도를 **대칭적으로 취급**하는 임계값 선택 기준. 의사결정 관점에서 암묵적 비용비가 **유병률에 의존**한다(Smits) | "비용 최적 임계값" |
| **임계값 0.5** | 6장 교란 실험의 **고정된 스트레스테스트 기준점**. 일관된 비교를 위한 규약 | "등가 비용의 근거" 또는 "비용 최적 임계값" |
| **비용 민감 평가** | 본 연구 범위에서 **수행하지 않음** (1A.4절) | — |

- 한편 4.3절의 Class Weight는 **클래스 불균형 보정**이지 오분류 비용 가중이 아니므로 혼동하지 않는다.
- **보고 지표 세트 (기술적 보고)**: 모든 탐지 성능 보고에는 **MCC, F1, Precision, Recall, PR-AUC**와 **혼동행렬**을 함께 제시한다.
  - 이들은 **기술적 보고(descriptive reporting)이며 개별 가설검정 대상이 아니다.** 따라서 다중비교 보정 대상은 주지표(MCC)에 한정한다.
  - 혼동행렬 제시 규칙은 8.1절 참고.
- 분류 임계값: 6장 교란 실험에서는 항상 0.5 고정. Phase 3 성능 산출에서는 Validation 기준 Youden's J로 조정 가능(조정 시 사유 기록, 임계값의 부트스트랩 95% CI 병기. CI 폭 > 0.2이면 임계값 불안정으로 보아 0.5 고정으로 회귀하고 사유 기록).
- 다중비교 보정: Benjamini-Hochberg 적용 (주지표 MCC 기준 비교에 한정)
- **개선 성공 판정 기준 (축별로 다름 — 지표 특성에 맞춰 사전 고정)**:
  - **MIA AUC_adv (5.4절 방향 보정 지표)**: 무작위 기준(0.5) 대비 **초과분의 20% 상대 감소**. 예: 베이스라인 AUC_adv 0.71(초과분 0.21) → 개선 후 초과분 0.168 이하(AUC_adv 0.668 이하)면 성공. (원시 AUC의 20% 감소가 아님)
    - *상대 기준을 쓰는 이유*: AUC는 0.5라는 명확한 무작위 기준점이 있어, 그 초과분을 기준으로 한 상대 감소가 해석 가능하다.
  - **탐지 MCC**: **절대 차이 ΔMCC ≥ 0.05**로 정의한다. (상대 변화율을 쓰지 않음)
    - *절대 기준을 쓰는 이유*: MCC는 [-1, 1] 범위의 상관계수이므로 상대 변화율이 불안정하다. 예컨대 0.05→0.10은 100% 개선이지만 실질 의미가 작고, 0.80→0.85는 6.25%에 불과하나 훨씬 중요할 수 있다. MCC가 0 근처이거나 음수인 경우 상대 변화 자체가 무의미해진다.
    - *내부 일관성*: 10.2절 허용 한도(MCC 절대 저하 0.05)와 10.4절 비열등성 마진 δ(0.05)가 모두 절대 0.05 스케일이므로, 프로토콜 전체가 MCC에 대해 단일 스케일로 작동한다.

- **코호트별 분리 보고 의무 (신설)**: Test 결과는 **통합 수치만 제시하는 것을 금지**하며, 반드시 **(a) 2017 악성, (b) 2024 악성, (c) 2024 정상** 세 코호트로 분리하여 병기한다. 공식 test 분할의 연도-라벨 교란(11.2.1절) 크기를 독자가 확인할 수 있게 하기 위함이며, 분할을 바꾸지 않으므로 추가 실험 비용이 없다.

### 8.1 혼동행렬 제시 규칙

- **대상 모델 선정 (사전 고정)**: **Validation MCC가 가장 높은 모델**의 혼동행렬을 본문에 제시한다. 어느 모델이 선정되는지는 결과에 따라 달라지나, **선정 규칙 자체가 사전 고정되어 있으므로 사후 선택에 해당하지 않는다.** 나머지 모델의 혼동행렬은 부록에 수록한다.
- **집계 방식**: 시드 5종(2.2절) 반복 결과의 **평균 행렬**을 제시한다. 각 칸은 5회 평균값이며, 소수점 첫째 자리까지 표기하고 표 각주에 "시드 5회 평균"임을 명시한다.
- **원본 접근성**: 시드별 개별 행렬은 부록 또는 보충자료로 제공하여, 독자가 원하는 어떤 지표든 직접 계산할 수 있도록 한다.
- 소표본: 신뢰구간 계산 기준을 완화하지 않는다. 결과를 "탐색적 결과"로 라벨링만 하고 계산 방식은 동일하게 엄격히 적용
- 부트스트랩: 재표집 단위는 독립 단위(바이너리/실행 그룹). 2,000회, 95% percentile CI

---

## 9. Phase 6 Test Set 및 MIA 평가셋 보호 원칙

- 탐지 성능: Phase 4 진단·Phase 6 개선안 선택은 Validation, 최종 평가는 Test
- MIA: 5.1절의 3분리(공격학습·개선안선택·최종감사)를 그대로 적용. 최종감사 ID는 Phase 4 시점에 고정, 이후 재열람하지 않는다

---

## 10. H5 선택적 개선 의사결정 규칙

### 10.1 진단축별 개선 후보 및 조건

| 진단 축 | 개선 후보 | 적용 조건(Validation) |
|---|---|---|
| 프라이버시 | 정규화+출력이산화 | **MIA AUC_adv** 95% CI 하한 > 0.5 |
| 강건성 | 교란학습 | 20% 조작 시 flip rate 증가폭 CI 하한 > 0 |
| 리소스 | 경량화 | 샘플당 지연시간 > 1ms |

### 10.2 판단 기준
- 개선 성공 판단: 해당 축 핵심지표의 개선 전후 차이 95% CI가 **8장에 축별로 정의된 개선 성공 기준**(MIA AUC는 초과분 20% 상대 감소, 탐지 MCC는 절대 차이 0.05)을 포함하고 0을 포함하지 않을 때
- 허용 한도: **탐지 MCC 절대 저하 0.05 초과**, 오탐률 상승 10%p 초과, 지연시간 50% 초과 증가 중 하나라도 차이-CI 상한을 넘으면 기각 (MCC는 [-1,1] 범위의 상관계수이므로 %p가 아닌 절대 차이로 규정)
- 타이브레이킹: (1) 목표축 개선폭 큰 순 → (2) 부작용 적은 순 → (3) 구현 복잡도 낮은 순
- 개선 실패 시: 미적용 및 실패 사실 그대로 보고

### 10.3 비교군 (Test에서 3자 비교)

**공통 출발 모델 = 모델 A (RF 기본)로 고정한다.**

```
                    Model A
                       │
          ┌────────────┴────────────┐
          ↓                         ↓
     선택적 개선                 일괄 개선
   (10.1~10.2 규칙)          (4.2절 순서·예산 고정)
          │                         │
          └────────────┬────────────┘
                       ↓
                   Test 최종 검증
```

1. **무개선 기준선** = 모델 A 그대로
2. **선택적 개선** = 모델 A + 진단 기반 선택 적용
3. **일괄 개선** = 모델 A + 3개 개선안 전부 적용

> **출발점을 A로 고정하는 이유**: H5가 묻는 것은 "개선 **전략**의 차이"이지 "모델의 차이"가 아니다. 출발 모델이 다르면 관측된 차이가 개선 전략 때문인지 모델 때문인지 분리할 수 없다. 모델 A는 4.1절에서 주 배포 후보이자 비교 기준점으로 지정되어 있으므로 출발점으로 적합하다.
>
> **모델 B·C·D의 Phase 6 미진입**: 위 고정에 따라 B·C·D는 Phase 6에 진입하지 않는다. 이들은 Phase 3~4(탐지 성능 + 3대 진단)까지 수행하여 H3 검증에 기여한다.

### 10.4 H5 검정 구조 — 선택적 개선 vs 일괄 개선 (신설)

> **수정 사유**: v2.2까지 10.2절은 "개선 **전후**" 차이를 판정했으나, H5의 주장은 "선택적 **vs 일괄**"이다. 검정 단위와 가설이 어긋나 있었으므로, H5를 직접 검정하는 규칙을 아래와 같이 사전 고정한다.

H5는 성격이 다른 두 주장으로 구성되므로 검정도 분리한다. **모든 비교는 Test에서 동일 평가 그룹에 대한 대응 비교로 수행하고, 차이 자체의 95% CI를 산출한다(8장).**

**지표 방향 규정 (필수 선행)** ⚠️

본 연구의 지표는 방향이 서로 다르다. 방향을 무시하고 하나의 부등식을 적용하면 **판정이 뒤집힌다.**

| 지표 | 좋은 방향 |
|---|---|
| 탐지 MCC | 높을수록 좋음 (higher-is-better) |
| MIA AUC_adv (5.4절) | 낮을수록 좋음 (lower-is-better) |
| 교란 flip rate | 낮을수록 좋음 |
| 추론 지연시간 | 낮을수록 좋음 |

**(a) 목표축 — 비열등성(non-inferiority) 검정**

```
Δ = Metric_selective − Metric_batch   (공통 정의)

[높을수록 좋은 지표: MCC]
  판정: Δ의 95% CI 하한 ≥ −δ   →  "일괄 대비 열등하지 않음"

[낮을수록 좋은 지표: MIA AUC_adv, flip rate, latency]
  판정: Δ의 95% CI 상한 ≤ +δ   →  "일괄 대비 열등하지 않음"
```

> 예시로 확인: MIA AUC_adv가 selective 0.60, batch 0.62라면 Δ = −0.02로 **selective가 더 안전**하다. 반대로 0.64 − 0.62 = +0.02는 더 위험하다. 따라서 낮을수록 좋은 지표는 **상한**을 δ와 비교해야 한다.

**(b) 부작용축 — 우위(superiority) 검정**

```
Δ = SideEffect_selective − SideEffect_batch

[낮을수록 좋은 부작용 지표]
  판정: Δ의 95% CI 상한 < 0   →  "부작용이 유의하게 더 적음"

[높을수록 좋은 지표가 부작용축인 경우]
  판정: Δ의 95% CI 하한 > 0   →  "유의하게 더 나음"
```

**H5 지지 조건 (둘 다 충족)**:
1. 모든 목표축에서 (a) 비열등성 충족
2. 최소 한 개 부작용축에서 (b) 우위 충족 **AND** 나머지 부작용축에서 비열등성 충족

**비열등성 마진 δ (사전 고정)**

| 축 | δ | 비고 |
|---|---|---|
| 탐지 MCC | 0.05 | 10.2절 허용 한도와 동일 수치 |
| MIA AUC_adv | 0.02 | 5.4절의 방향 보정 지표 AUC_adv 기준 (낮을수록 좋음) |
| 교란 flip rate | 5%p | |
| 추론 지연시간 | 20% | 허용 한도(50%)보다 엄격 — 비열등 판정은 더 보수적이어야 함 |

> **개념 구분 명시**: δ(비열등성 마진)와 10.2절 허용 한도는 **다른 개념**이다. 허용 한도는 "이 이상 나빠지면 해당 개선안을 기각"하는 기준이고, δ는 "이 정도 열등까지는 동등으로 간주"하는 기준이다. 탐지 MCC에서 두 값이 0.05로 같은 것은 프로토콜 내부 일관성을 위한 **의도적 선택**이며, 두 개념이 동일해서가 아니다.
>
> **δ 값의 성격**: 위 수치들은 이론적으로 도출된 값이 아니라 본 연구가 사전 고정한 판단 기준이다. 논문에 이 사실을 명시하고, 다른 δ를 적용했을 때의 결론 변화를 민감도 분석으로 부기할 수 있다.

**3분 판정 규칙 (판정 불가 포함)**

> **v2.3의 "CI 폭 > 2δ → 판정 불가" 규칙은 폐기한다.** CI가 넓다는 것만으로 판정 불가가 되지는 않는다. 예컨대 δ=0.05이고 MCC의 Δ CI가 [0.10, 0.50]이면 폭은 0.40으로 2δ(0.10)를 크게 초과하지만, 하한 0.10 > −0.05이므로 **비열등성은 이미 확립된다.** 판정 가능 여부는 CI 폭이 아니라 **CI가 비열등성 경계(±δ)를 가로지르는지**로 정해진다.

```
[높을수록 좋은 지표: MCC]
  CI 하한 ≥ −δ            →  비열등
  CI 상한 <  −δ            →  열등
  CI가 −δ를 가로지름        →  판정 불가

[낮을수록 좋은 지표: MIA AUC_adv, flip rate, latency]
  CI 상한 ≤ +δ            →  비열등
  CI 하한 >  +δ            →  열등
  CI가 +δ를 가로지름        →  판정 불가
```

판정 불가인 경우 **H5를 지지도 기각도 하지 않고**, 관측된 점추정치와 CI를 그대로 보고하며 검정력 한계를 논문에 명시한다. 판정 불가를 회피하기 위해 사후에 δ를 넓히거나 시드를 추가하지 않는다.

---

## 11. 데이터셋 확정 및 교차검증 설계

### 11.1 주실험 데이터셋 — DMBD 2025로 교체 (2026-09-18 확정)

> **경위**: 11.1절 사전 규칙에 따라 2026-09-18 기한 도래로 CIC-MalMem-2022가 주실험 데이터셋으로 자동 전환되었으나, Phase 1~2 데이터 검증 과정에서 **해당 데이터셋이 환경 지문(environmental artifact)으로 거의 완전히 분리됨**을 확인했다(11.3절). 이에 주실험 데이터셋을 **DMBD 2025(LLNL)**로 재교체한다. 이 결정은 결과를 보고 유리한 데이터셋을 고른 것이 아니라, **데이터 구조 진단 결과 기존 데이터셋이 연구 목적에 부적합함이 드러나** 내린 것이며, 근거를 11.3절에 정량적으로 기록한다.

| 역할 | 데이터셋 |
|---|---|
| **주실험** | **DMBD 2025** (Dynamic Malware Behavior Dataset, LLNL) |
| **재현 검증** | DMBD 내부 **코호트 간**(2017 vs 2024) — 11.2절 |
| **사례 연구** | CIC-MalMem-2022 — 벤치마크 타당성 문제의 실증 사례로만 사용(11.3절). 모델 학습·진단 대상 아님 |

**DMBD 구조 (Phase 1·2 확인 완료)**

| 항목 | 값 |
|---|---|
| 집계 단위 | `experiment` = 1회 실행. **정상·악성 양쪽 모두 존재** |
| 유효 표본 | **64,747 실행** (공식 65,416 중 669개는 이벤트 0건) |
| 클래스 | 악성 39,498(61.0%) / 정상 25,249(39.0%) |
| 이벤트 종류 | `process_create`, `process_term`, `image_load`, `net_connect`, `file_create`, `reg_create_key`, `reg_write`, `reg_delete_key`, `reg_delete_value` (+ `detonation` 1건/실행) |
| 결측 | `year` 1건 |
| 라벨링 | 약 80개 AV 엔진의 합의 투표 |

**artifact 부재 확인**: 깊이 1 결정트리 76.33%, 깊이 3 85.20%(5-fold CV). 다수 클래스 기준선 61.0% 대비 단일 규칙 이득은 +15.3%p로, CIC-MalMem-2022의 +49.7%p와 대조된다.

**DMBD가 본 프로토콜에 적합한 구조적 이유**: 데이터셋 문서에 따르면 **폭발(detonation) 결과로 발생한 이벤트만 포함하고 정상 Windows 백그라운드 활동은 제거**되어 있다. 즉 VM 전체 상태가 아니라 실행된 샘플의 프로세스 트리만 담고 있어, CIC-MalMem에서 발생한 환경 지문 문제가 구조적으로 발생할 수 없다.

### 11.2 재현 검증 설계 — 코호트 간 재현 (H4 재정의)

> **변경 사유**: v2.5까지 H4는 "CIC-MalMem → DMBD 교차 데이터셋 재현"이었으나, CIC-MalMem이 artifact 사례 연구로 역할이 바뀌면서 두 번째 유효 데이터셋이 소멸했다. artifact가 지배하는 데이터셋에서 나온 결과는 "재현 여부"를 해석할 수 없으므로 교차검증 대상으로 쓸 수 없다.

**재정의**: DMBD 내부의 **연도 코호트**를 재현 검증 축으로 사용한다.

- **코호트 A (2017)**: 학습 데이터의 주축이며 test 악성의 87.8%
- **코호트 B (2024)**: 학습셋에 악성이 전혀 없는 시간적 홀드아웃

H4는 "**한 코호트에서 관찰된 취약성 패턴이 다른 코호트에서도 나타나는가**"를 묻는다. 이는 데이터셋 간 일반화가 아니라 **시간적 일반화**이며, DMBD의 원래 설계 의도(2024 샘플을 학습에서 배제한 temporal challenge)와 부합한다.

**한계 명시 의무**: 단일 데이터셋 내부 비교이므로, 서로 다른 수집 환경·도구를 넘나드는 일반화는 검증되지 않는다. 논문 한계점에 "cross-dataset validation 미수행"을 명시한다.

### 11.2.1 공식 분할 보존과 연도-라벨 교란

DMBD 공식 train/test 분할을 **보존**한다(공표된 베이스라인과의 비교 가능성 확보).

**단, 공식 test 분할에는 심각한 연도-라벨 교란이 존재하며 이를 반드시 명시한다.**

| test 구성 | 2017 | 2024 |
|---|---|---|
| 정상 | 0 | **5,248 (100%)** |
| 악성 | **17,131 (87.8%)** | 2,367 |

즉 **test의 정상은 전부 2024년, 악성은 대부분 2017년**이다. "2017년 특성 → 악성"이라는 연도 단서만으로도 높은 점수가 가능하다. (독립 재현 연구 역시 test의 69.2%가 2017 악성이며 해당 구간에 오분류할 정상 샘플이 0개임을 지적했다.)

> 참고로 데이터셋 문서는 "2024년 **악성** 샘플이 학습셋에 없다"고 기술하나, **2024년 정상은 학습셋에 7,004건 존재**한다. 시간적 홀드아웃은 악성 클래스에만 적용되어 있다.

**필수 보완 — 코호트별 분리 보고**: Test 결과를 통합 수치로만 제시하지 않고, 반드시 **(a) 2017 악성, (b) 2024 악성, (c) 2024 정상** 세 코호트로 분리하여 병기한다. 분할을 변경하지 않으므로 추가 실험 비용이 없으며, 교란의 크기를 독자가 직접 확인할 수 있게 한다. 통합 수치만 제시하는 것은 금지한다.

**Validation 확보**: 공식 train(40,000)을 Train/Validation으로 재분할한다. 비율은 Train 75% / Validation 25%(전체 대비 약 46%/15%/38%). 분할 시 2.1절 원칙을 적용한다.

### 11.2.2 Train↔Test 사전확률 이동 (Phase 3 신규 발견, 2026-09-19)

연도-라벨 교란(11.2.1절)과 **별개로**, 공식 분할에는 클래스 사전확률의 큰 이동이 존재한다.

| 분할 | 정상 | 악성 | 악성 비율 |
|---|---|---|---|
| 공식 train (Train+Validation) | 20,000 | 20,000 | **50.0%** |
| 공식 test | 5,249 | 19,498 | **78.8%** |

**영향**:
- **임계값 이식 불가**: Validation(사전확률 50%)에서 선택한 임계값은 Test(78.8%)에서 최적이 아니다. Phase 6 최종 평가 시 이 점을 명시하고, 임계값을 Test에서 재조정하지 않는다(Test 보호 원칙, 9장).
- **PR-AUC 비교 불가**: PR 곡선의 기준선은 양성 비율이므로 Validation과 Test의 PR-AUC를 직접 비교하지 않는다(8장 주의사항의 구체적 사례).
- **MCC는 상대적으로 강건하나 영향에서 자유롭지 않다**: 혼동행렬 네 칸을 모두 쓰므로 정확도보다 낫지만, 사전확률 변화의 영향을 받는다.

**보고 의무**: Phase 6 Test 결과 서술 시 이 사전확률 이동을 명시하고, 11.2.1절의 코호트별 분리 보고와 함께 제시한다.

---

### 11.3 사례 연구 — CIC-MalMem-2022의 벤치마크 타당성 문제

본 연구는 CIC-MalMem-2022를 **모델 학습·진단에 사용하지 않으며**, 아래 발견을 독립된 사례 연구로 보고한다.

| 진단 | 결과 |
|---|---|
| 깊이 1 결정트리 (5-fold CV) | **99.65%** |
| 깊이 3 | 99.84% |
| 단일 특징 최고(`svcscan.nservices`) | **99.66%** |
| 단일 특징 95% 이상 | 17개 |

**원인**: 정상 덤프는 `svcscan.nservices`가 거의 항상 **395**, 악성은 **389**이다. `svcscan.kernel_drivers`는 **222 vs 221**로 드라이버 1개 차이다. 평균 차이는 1~2%에 불과하나 분포가 거의 겹치지 않아, 이는 악성 행위가 아니라 **정상 수집 VM과 악성 수집 VM의 기본 시스템 상태 차이**를 반영한다.

**의의**: 이 데이터셋을 사용한 선행 연구들은 99.9%, 99.97%, 99.99%, 100%의 정확도를 일관되게 보고하나, 본 연구가 확인한 바로는 **깊이 1 결정트리 하나가 99.65%에 도달**한다. 이는 모델의 우수성이 아니라 **벤치마크의 타당성 문제**이며, H1("높은 탐지 성능이 곧 안전을 의미하지 않는다")의 극적인 실증 사례로 논문에 보고한다.

**보고 시 주의**: 본 연구는 CIC-MalMem-2022에서 모델을 학습하지 않았으므로, 선행 연구의 구체적 결과를 재현·반박했다고 주장하지 않는다. 주장 범위는 "공개된 데이터에 단순 진단을 적용한 결과 자명한 분리 가능성이 확인되었다"로 한정한다.

---

## 12. 중단·대체·제외 기준

| 조건 | 처리 방식 |
|---|---|
| 특정 특징이 공통 정의 불가/결측 30%+/의미 상이 | 공통특징에서 제외 또는 데이터셋별 개별 특징 유지 |
| 교란 표본이 6.2절 검증가능 기준을 벗어남 | "무효 변형"으로 집계, 성공률 계산 제외 |
| B·C 모델과 A의 탐지 성능 차이의 CI가 0을 포함 | "본 실험에서 통계적으로 유의미한 차이를 발견하지 못함"으로 기술한다. **Phase 6 진입 여부와는 무관하다** — 10.3절에 따라 Phase 6의 출발 모델은 A로 고정되어 있으므로 B·C는 애초에 Phase 6에 진입하지 않으며, 탐지 성능 차이 유무가 개선안 적용을 좌우하지 않는다 |
| 주실험 데이터셋(CIC-MalMem-2022)의 다중 스냅샷 집계방식별 결과 차이의 CI가 0을 불포함 | 집계방식별 결과 병기, 대표값 임의선택 금지 |
| 모델 미수렴/과적합(Train-Val MCC 절대 차이 > 0.20) | 4.2절 범위 내 재조정만 허용, 범위 밖이면 해당 축 제외+사유기록 |
| 리소스 측정환경 재현 불가 | 상대비율 중심 보고, 측정환경 사양 명시 |
| DMBD 변환 후 그룹 수 < 30 | 탐색적 결과로 한정, 통계기준은 동일하게 유지 |
| MIA 공격자 모델 불안정(시드간 AUC 변동 > 0.1) | 5.3절 2순위로 자동전환, 축 제외 없음 |
| MIA 옵션A 적용조건(family 구조 존재) 미충족 | 5.2절 옵션B로 자동 전환, confound 명시 문구 사용. **DMBD에서 적용 확정** |

---

## 13. 전체 Phase 흐름 및 산출물

| Phase | 내용 | 주요 산출물 | Phase 1 이후 확정 필요 항목 |
|---|---|---|---|
| 0 | 프로토콜 확정 | 본 문서 | - |
| 1 | 데이터 구조 파악 | ✅ **완료 (2026-09-18)** — CIC-MalMem-2022 조사: 58,596행/55특징/정상 실행ID 부재 확인 (이후 Phase 2에서 artifact 발견으로 사례연구로 전환) | 해당 결과는 11.3절·3.2.1절에 사례연구 기록으로 보존 |
| 2 | 공통 특징 확정 | ✅ **완료 (2026-09-18)** — DMBD 특징 14개 확정(제외 2개), 난이도 분류 재작성, 발생률 정규화 미적용 결정, artifact 진단으로 주실험 데이터셋 교체 | ✅ 확정: 분석단위=실행(3.2절), 분할(2.1절), 불균형 Class Weight(4.3절), MIA 옵션 B+shadow(5장), H4 코호트 재정의(11.2절) |
| 3 | 4개 모델 구축 | ✅ **완료 (2026-09-19)** — A/B/C/D Validation 성능, 차이 CI, 임계값 분석 | ✅ 확정: BO=GP-EI, Class Weight 미적용, MLP 기본값, max_features [1,12], H2 n=7. 신규: 사전확률 이동(11.2.2절) |
| 4 | 3대 진단 + 특징중요도 | MIA·교란민감도·리소스 결과 | - |
| 5 | DMBD 독립 재현 | H4 축별 재현판정 | - |
| 6 | 선택적 개선 | 3자 비교 | - |
| 6A | (부가·탐색적) 모델 복잡도 스윕 | 복잡도-4축 트레이드오프 곡선 (Validation 기준) | 우선순위 최하, 시간 여유 시에만 수행 (13A절) |
| 7 | 논문 작성 | 최종 원고 | - |

---

## 13A. 부가 분석: 모델 복잡도 스윕 (탐색적, 우선순위 낮음)

> **성격 선언**: 본 분석은 **탐색적(exploratory) 분석**이며, H1~H5와 같은 확증적 가설 검정이 아니다. 따라서 8장의 다중비교 보정(Benjamini-Hochberg) 대상에 **포함하지 않으며**, 결과 서술 시 "관찰되었다" 수준으로 기술하고 "입증되었다"는 표현을 사용하지 않는다. 이 선언을 사전에 두는 이유는, 확증적 가설에 항목을 추가하면 보정 부담이 커져 기존 H1~H5의 검정력이 저하되기 때문이다.

### 13A.1 목적

경량화(모델 복잡도 축소)가 리소스 효율성뿐 아니라 **다른 진단 축에도 동시에 영향을 미치는지**를 관찰한다. 특히 모델이 단순해지면 학습 데이터에 대한 암기가 줄어 멤버십 추론 취약성도 함께 감소할 가능성이 있는데, 이는 H1("탐지 성능이 높다고 다른 축이 함께 보장되지는 않는다")의 역방향 사례 — 즉 **하나의 조치가 여러 축을 동시에 움직이는 경우** — 에 해당하여 논의 가치가 있다.

### 13A.2 Phase 6(선택적 개선)과의 구분 ⭐

| | Phase 6 경량화 (10.1절) | 13A 복잡도 스윕 |
|---|---|---|
| 성격 | 진단된 취약점에 대한 **처방** | 복잡도-성능 **관계 탐색** |
| 발동 조건 | Validation 지연시간 > 1ms일 때만 | 조건 없이 수행 (시간 여유 시) |
| H5와의 관계 | **H5 검증 대상** | H5와 무관 |
| 평가 데이터 | Test (최종 검증) | **Validation만 사용** |

> **Test 보호**: 본 분석은 Validation에서만 수행한다. 탐색적 분석에 Test를 사용하면 9장 Test 보호 원칙이 무너진다. 13A의 어떤 결과도 Phase 6 개선안 선택에 영향을 주지 않는다(이미 10.1절 규칙으로 고정되어 있으므로).

### 13A.3 스윕 설계 (사전 고정)

**변화시킬 파라미터와 단계** — 결과를 보고 단계를 추가·제거하지 않는다:

| 모델 | 스윕 파라미터 | 단계 |
|---|---|---|
| RF (A) | `n_estimators` | 20 / 50 / 100 / 200 / 300 (5단계), `max_depth`는 기본값 고정 |
| MLP (D) | 은닉 유닛 수 | 기준 대비 30% / 50% / 70% / 100% (4단계), 층 수는 고정 |

- 각 단계에서 **4축 지표를 모두 동시에 측정**한다: 탐지 MCC(주지표) / MIA AUC / 20% 교란 시 flip rate / 샘플당 추론 지연시간
- 시드 5종(2.2절) 반복, 평균±95% CI 보고
- MIA는 5.1절의 **공격 학습·임계값 보정용 분할**만 사용한다. 최종 감사용 ID는 열람하지 않는다(9장)

### 13A.4 보고 형식

- 가로축을 복잡도(트리 수/유닛 수), 세로축을 각 축 지표로 하는 **트레이드오프 곡선**으로 제시
- 지표 간 스케일이 다르므로 각 지표를 최대 복잡도 시점 대비 상대값으로 정규화한 보조 그림을 병기
- 축 간 상관은 Spearman 상관계수로 보고하되, **인과 주장은 하지 않는다** (복잡도가 유일한 변화 요인이 아닐 수 있음)

### 13A.5 수행 조건 및 생략 기준

- **우선순위**: 최하. Phase 3~6이 모두 완료되고 시간 여유가 있을 때만 수행한다
- **생략 시 처리**: 수행하지 못한 경우 논문에 언급하지 않는다(수행하지 않은 분석을 "향후 과제"로 포장하지 않음). 단, 14장 변경이력에는 "시간 제약으로 미수행"으로 기록한다
- **중단 기준**: 스윕 도중 특정 단계에서 모델이 수렴하지 않으면(12장 기준) 해당 단계만 제외하고 나머지를 보고하며, 제외 사실을 명시한다

---

## 14. 변경 이력 및 예외 기록 규칙

| 발생 조건 | 원래 계획 | 실제 처리 | 변경 이유 | 영향 범위 | 재현 정보(seed/버전/파라미터) |
|---|---|---|---|---|---|
| 2026-09-15 / IDS-ML 환경 구축 | 로컬에서 IDS-ML 노트북 실행하여 파이프라인 구조 파악 | 실행 포기 후 `.py` 변환하여 코드 분석으로 전환 | 2019년 코드와 최신 라이브러리 간 버전 충돌 4건(pandas dtype 2건, imblearn API 2건) 발생. 목적이 "실행 결과"가 아닌 "구조 파악"이므로 코드 읽기로 충분 | Phase 0 준비 단계에 한정, 실험 결과에 영향 없음 | Python 3.13.15, scikit-learn 1.9.1, Windows 11 / nbconvert 사용 |
| 2026-09-15 / IDS-ML 코드 분석 | 4.2절 BO 설정을 "베이지안 최적화, 30회"로만 기술 | BO-TPE(hyperopt) 알고리즘 명시 + RF 탐색공간 구체화 (4.2절) | IDS-ML에서 검증된 탐색 범위를 채택하여 임의 설정을 줄임 | 4.2절, Phase 3 이후 모델 B·C | hyperopt TPE, max_evals=30 |
| 2026-09-15 / IDS-ML 코드 분석 | (해당 없음 — 신규 기록) | 4.2.1절 신설: 선행 구현 대비 의도적 상이점 3건 문서화 | 선행 연구에서 데이터 누수 패턴 2건(스태킹 in-sample 예측, BO 목적함수에 Test 사용) 발견. 본 프로토콜이 이를 이미 방지하고 있음을 확인하고 근거로 기록 | 4.2.1절, 논문 방법론 서술 | Tree-based_IDS_GlobeCom19.py L320-327, MTH_IDS_IoTJ.py L453-455 |
| 2026-09-16 / 앙상블 이론 문헌 조사 | (해당 없음 — 범위 유지 결정) | 편향-분산 분해(Domingos 2000)를 **이론 인용 및 결과 해석 근거로만 사용**, 실측은 수행하지 않음 | 0-1 손실에 직접 적용 가능한 분해 이론을 확보했으나, 실측 추가 시 사전등록 시점의 실험 범위를 벗어남. 범위 유지를 우선 | 논문 2장·결과 해석 서술. 실험 절차 변경 없음 | Domingos(2000) AAAI/ICML / mlxtend bias_variance_decomp 미사용 |
| 2026-09-19 / Phase 3 — BO 알고리즘 대체 | hyperopt의 BO-TPE (4.2절) | **GP 기반 BO(Matérn 2.5 + Expected Improvement) 직접 구현으로 대체.** 탐색 공간·시행 횟수(30회)·목적함수는 불변 | 실행 환경에 hyperopt 미설치이며 네트워크 차단으로 설치 불가. GP-EI는 Frazier(2018) 튜토리얼이 설명하는 표준 BO 형태이며, TPE 채택 사유였던 "IDS-ML 탐색 공간"은 그대로 유지됨 | 4.2절, 모델 B·C | 초기 무작위 10 + BO 20, 최적 CV MCC=0.8360 |
| 2026-09-19 / Phase 3 — Class Weight 판정 정정 | 소수 클래스 39.0% < 40% → Class Weight 적용 (v2.6) | **미적용으로 정정** | 전체 데이터 기준으로 판정한 오류. 클래스 가중은 학습 시 적용되므로 Train 분포로 판정해야 하며, 공식 train은 정확히 50:50 | 4.3절, Phase 3 이후 전체 | Train 20,000/20,000 |
| 2026-09-19 / Phase 3 — MLP 설정 확정 | 은닉층 1~2개, 유닛 [16,128] (탐색 주체 불명확) | **미튜닝 + scikit-learn 기본값 (100,) 고정** | A(미튜닝 RF)와 D의 대칭성 확보. 임의값 (64,) 대신 라이브러리 기본값을 근거로 삼아 선택 자의성 제거 | 4.2절, 모델 D, H3 해석 | 재실행 결과 MCC 0.6309 |
| 2026-09-19 / Phase 3 — 스태킹 베이스 교체 | Gradient Boosting 1종 | **HistGradientBoosting으로 대체** | 연산 시간 제약(OOF 5-fold × 5시드). 동일 알고리즘 계열의 고속 구현이며 성능 특성 유사 | 4.2절, 모델 C | 시드당 약 42초 |
| 2026-09-19 / Phase 3 — max_features 상한 확정 | [1, 20] | **[1, 12]** | 중복 제거 후 최종 특징 수가 12개이므로 상한을 실제 차원에 맞춤 | 4.2절 | — |
| 2026-09-19 / Phase 3 — H2 분석 단위 축소 | 교란 대상 "쉬움" 9개 (n=9) | **중복 제거로 2개 탈락, n=7** | Train에서 n_events~n_uniq_cguid ρ=1.000, ~n_uniq_child ρ=0.983. 결정론적 규칙에 따라 교란 대상 2개가 탈락하고 교란 제외 항목(n_events)이 잔존 | 6.3·6.4절, H2 검정력 | **규칙을 사후 변경하지 않음**, 한계로 기록 |
| 2026-09-19 / Phase 3 — 사전확률 이동 발견 | (해당 없음 — 신규 발견) | **11.2.2절 신설** | 공식 train은 50:50이나 test는 악성 78.8%. 연도 교란과 별개의 문제로, 임계값 이식·PR-AUC 비교에 직접 영향 | 11.2.2·8·9장, Phase 6 | Test 재조정 금지 원칙 유지 |
| 2026-09-18 / Phase 2 — CIC-MalMem artifact 발견 | CIC-MalMem-2022를 주실험 데이터셋으로 사용 (11.1절 2순위) | **주실험을 DMBD 2025로 재교체.** CIC-MalMem은 11.3절 artifact 사례연구로만 사용 | 깊이 1 결정트리가 99.65% 달성. svcscan.nservices가 정상 395/악성 389로 거의 결정적 분리 → 악성 행위가 아닌 VM 환경 상태를 학습. 이 데이터로는 4축 진단이 전부 artifact 탐지에 관한 것이 됨 | 11장 전체, 2.1·3.2·3.3.2·4.3·5·6.3·8·13장 | DMBD 깊이1 76.33% vs CIC-MalMem 99.65% |
| 2026-09-18 / Phase 2 — 분석 단위 복원 | 1행(메모리 덤프) = 1샘플 (v2.5) | **1 실행(experiment) = 1샘플로 복원** | DMBD는 experiment 필드가 정상·악성 양쪽에 존재하여 대칭 적용 가능. v2.5의 강등은 CIC-MalMem의 구조적 결함에 대응한 것이었음 | 3.2·2.1·3.4절 | 64,747 실행 |
| 2026-09-18 / Phase 2 — H4 재정의 | 교차 데이터셋 재현(CIC-MalMem → DMBD) | **코호트 간 재현으로 재정의** (DMBD 2017 vs 2024) | CIC-MalMem이 사례연구로 전환되며 유효한 두 번째 데이터셋 소멸. artifact 지배 데이터셋의 결과는 재현 여부 해석 불가 | H4, 1.2·11.2절 | cross-dataset 미수행을 한계로 명시 |
| 2026-09-18 / Phase 2 — 공식 분할 교란 | 공식 train/test 분할 보존 | **보존 유지 + 코호트별 분리 보고 의무 신설** | test 정상은 100% 2024년, test 악성은 87.8%가 2017년으로 연도-라벨 교란 존재. 분할 변경 없이 교란 크기를 노출시키는 방법 선택 | 11.2.1·8장 | 추가 실험 비용 없음 |
| 2026-09-18 / Phase 2 — 발생률 정규화 폐기 | 분당 발생률(events/min)로 정규화 | **원시 카운트 사용**, duration_s는 별도 특징으로 유지 | duration_s 중앙값 1.36초, p10 0.04초, 0 이하 401건으로 나눗셈이 불안정. 명목 관측 창이 5분으로 동일하므로 원시 카운트가 이미 비교 가능 | 3.3.2·1A.2절 | 공식 베이스라인도 원시 카운트 사용 |
| 2026-09-18 / Phase 2 — 난이도 분류 재작성 | 가상의 발생률 특징 기준 난이도표 | **실제 14개 특징 기준으로 전면 재작성** (쉬움 9 / 어려움 4 / 제외 1) | v2.5 난이도표가 실제 특징과 불일치. 6.2절 판정 기준은 불변, 대상만 교체 | 6.3·6.4절, H2 (n=9) | n_events는 파생합이라 교란 제외 |
| 2026-09-18 / Phase 2 — MIA·불균형 조건 재판정 | MIA 옵션 A, 불균형 미적용 (CIC-MalMem 기준) | **MIA 옵션 B로 전환**(family 구조 부재, 연도 분포로 매칭), **Class Weight 적용**(소수 39.0% < 40%) | 데이터셋 교체로 사전 조건문 재평가. 결과를 보고 고른 것이 아님 | 4.3·5.1·5.3절 | shadow-model 1순위 유지 |
| 2026-09-18 / Phase 2 — family 평가 불가 | 미지 family 탐지를 기본 설정으로 | **시간적 일반화(코호트 간) 평가로 대체** | DMBD는 malware family 라벨을 제공하지 않음 | 2.1·11.2절 | 한계로 명시 |
| 2026-09-18 / Phase 1 — 정상 샘플 실행ID 부재 | 3.2절 "1 실행 = 1 샘플", 3.3절 스냅샷 집계, 3.3.1절 감염시점 이중설정 | **3.2절 재확정: 1행(메모리 덤프) = 1샘플.** 3.3·3.3.1절 폐기. 3.2.1절 신설(복원 불가 검정 근거) | 정상 29,298행의 Filename이 전부 "Benign"으로 실행ID 부재. 악성만 집계하면 std=0이 정상을 완벽 판별하는 치명적 누수 발생. 파일순서 기반 복원도 검정으로 불가 확인(인접행 거리 악성 0.268 vs 정상 0.782, 10주기 구조 없음) | 3.2·3.2.1·3.3·3.3.1·3.4절, 2.1절, 6.4절 n 산정 | MalMem2022.csv, 58,596행×58열 |
| 2026-09-18 / Phase 1 — 분할 설계 | family 비중복 그룹 분할(대칭) | **2.1절 비대칭 그룹 분할** — 악성은 실행+family 단위, 정상은 행 단위 무작위 | 정상에 그룹 구조가 없어 대칭 적용 불가. 악성만이라도 실행 단위 분할로 누수 차단 | 2.1절, 4.2.1절 | 악성 2,906 실행 / 15 family(141~229 실행) |
| 2026-09-18 / Phase 1 — 특징 구조 | 3.3절 "집계 후 30개 초과 시 중복 제거" | **3.3.2절로 이관, 원 특징 55개 기준 적용.** 상수 3개 사전 제거 | 집계 폐기로 기준이 무의미해졌으나 원 특징에 강한 중복 존재(|ρ|>0.95 쌍 40개, 일부 ρ=1.000) | 3.3.2절, 3.4절 | Spearman, 컬럼순서 후자 제거 |
| 2026-09-18 / Phase 1 — 불균형·MIA 조건 확정 | 조건부 규칙(4.3절 40% 기준, 5.1절 30% 기준, 5.3절 100 기준) | **불균형 처리 미적용**(50:50 균형), **MIA 옵션 A 적용**(15 family 전부 조건 충족), **shadow-model 1순위 적용**(그룹 수 2,906/29,298) | 사전 정의된 조건문을 데이터에 적용한 결과 | 4.3·5.1·5.3절 | 결과를 보고 고른 것이 아니라 조건문 실행 |
| 2026-09-18 / Phase 1 — 난이도 분류 불일치 | 6.2~6.3절 난이도표가 "분당 발생률" 특징 전제 | **Phase 2에서 실제 55개 특징 대상으로 전면 재작성** 예정으로 표기. 판정 기준·규칙은 불변 | 실제 특징은 VolMemLyzer 정적 카운트 지표(pslist/handles/psxview 등). 네트워크로 상정했던 handles.nport는 상수 | 6.2·6.3·6.4절, H2 | 미해결 항목 |
| 2026-09-18 / 2차 검토 ① H5 방향 | 모든 축에 "CI 하한 ≥ −δ" 단일 부등식 적용 | **지표 방향별로 부등식 분리** — 높을수록 좋은 지표(MCC)는 CI 하한 ≥ −δ, 낮을수록 좋은 지표(AUC_adv/flip/latency)는 CI 상한 ≤ +δ | 방향을 무시하면 판정이 뒤집힘. MIA AUC가 낮아지는 것이 개선인데 하한 기준을 적용하면 오판 | 10.4절 (a)(b) 전체 | 방향 규정표 추가 |
| 2026-09-18 / 2차 검토 ② MIA 방향 | MIA AUC 원시값 사용 | **5.4절 신설 — AUC_adv = max(AUC, 1−AUC)** 도입, 8장·10.1·10.4·1.2절에 일괄 적용 | AUC 0.40은 공격자가 판정을 뒤집으면 0.60이 되므로 "더 안전"이 아님. 0.5로부터의 거리가 실질 공격 가능성 | 5.4절, 8장, 10.1절, 10.4절, 1.2절 | 원시 AUC 병기 의무 |
| 2026-09-18 / 2차 검토 ③ 판정불가 규칙 | "CI 폭 > 2δ → 판정 불가" | **규칙 폐기.** CI가 ±δ 경계를 가로지르는지로 3분 판정(비열등/열등/판정불가) | CI가 넓어도 판정 가능한 경우가 있음. 예: δ=0.05, CI [0.10,0.50]은 폭 0.40이지만 하한 0.10 > −0.05로 비열등 확립 | 10.4절 | 수학적 오류 수정 |
| 2026-09-18 / 2차 검토 ④ Phase 6 출발점 | 선택적/일괄 개선의 출발 모델 미명시 | **10.3절 — 출발 모델을 Model A로 고정**, 다이어그램 추가. B·C·D는 Phase 6 미진입 | 출발 모델이 다르면 관측 차이가 개선 전략 때문인지 모델 때문인지 분리 불가 | 10.3절, 12장 | H5는 "전략의 차이"를 묻는 가설 |
| 2026-09-18 / 2차 검토 ⑤ H2 분석단위 | 분석 단위 미명시, 기울기 부호 모호 | **6.4절 — 분석 단위를 최종 입력 특징 컬럼(n≈12)으로 고정**, S_i = −Δp/Δreduction 부호 정의 | 기저 행위 단위면 "쉬움" 3종뿐이라 n=3으로 상관 분석 불가. 부호 미정의 시 상관 방향 해석 불가 | 6.4절, H2 가설행 | 최종 n 보고 의무, 경향 관찰로 서술 |
| 2026-09-18 / 2차 검토 ⑥ BCCC 잔재 | H4·1A.1·1A.2·12장에 BCCC가 현재 절차로 남아 있음 | **현재 절차 문장을 CIC-MalMem-2022 기준으로 통일.** BCCC는 14장 변경이력에만 역사적 기록으로 유지 | 11.1절에서 데이터셋이 전환되었으나 본문 일부가 갱신되지 않았음 | H4 가설행, 1A.1, 1A.2, 12장 | — |
| 2026-09-18 / 2차 검토 ⑦ nested CV 용어 | "Train 내부 중첩 교차검증(nested CV)" | **"Train 내부 3-fold 교차검증"으로 표현 수정**, nested CV가 아님을 명기 | nested CV는 inner/outer 이중 CV 구조를 뜻함. 본 설계는 Train 내부 CV + 별도 Validation/Test의 hold-out 구조 | 2.3절, 4.2절 | 용어 정확성 |
| 2026-09-18 / 2차 검토 ⑧ 비용 표현 | MCC·Youden's J·0.5를 "등가중 비용 가정 공유"로 묶어 서술 | **세 요소의 성격을 표로 분리.** 0.5는 스트레스테스트 기준점, Youden's J는 대칭적 취급(비용비는 유병률 의존), MCC는 균형 반영 지표로 규정 | 0.5 임계값 자체가 FP/FN 등가 비용을 의미하지는 않음. Youden's J도 Smits 지적대로 암묵 비용비가 유병률에 의존 | 8장, 1A.4절 연결 | "비용 최적 임계값"이라는 서술 금지 |
| 2026-09-18 / 2차 검토 ⑨ 12장 B·C 규칙 | "B·C와 A의 차이 CI가 0을 포함 → Phase 6 생략" | **Phase 6 진입 여부와 무관함을 명시**하고 규칙의 목적을 탐지 성능 서술로 한정 | 탐지 성능 차이 유무와 MIA/강건성/리소스 진단 기반 개선은 논리적으로 연결되지 않음. ④ 결정으로 B·C는 애초에 Phase 6 미진입 | 12장 | — |
| 2026-09-18 / 외부 검토 반영 ① H5 | 10.2절이 "개선 전후" 차이를 판정 (H5는 "선택적 vs 일괄"을 주장) | **10.4절 신설** — 목표축 비열등성 + 부작용축 우위의 이중 검정 구조, δ 사전 고정, 검정력 부족 시 판정불가 규칙 | 검정 단위와 가설이 어긋나 있었음. H5를 직접 검정하는 규칙 부재 | 10.4절, H5 가설표 | δ: MCC 0.05 / MIA AUC 0.02 / flip 5%p / latency 20% |
| 2026-09-18 / 외부 검토 반영 ② MCC 기준 | MCC "20% 상대 개선" (v2.1에서 도입) | **절대 차이 ΔMCC ≥ 0.05로 변경.** MIA AUC는 초과분 20% 상대 감소 유지 | MCC는 [-1,1] 상관계수라 상대 변화율이 불안정(0.05→0.10이 100%). 10.2절 허용한도(절대 0.05)·10.4절 δ와 내부 불일치도 존재했음 | 8장, 10.2절 | 축별 기준 상이 사유를 8장에 명기 |
| 2026-09-18 / 외부 검토 반영 ③ 감염시점 | 감염시점 라벨 존재 시 감염 후 구간만 집계 | **3.3.1절 신설** — 설정 A(전체 스냅샷, 주)와 설정 B(감염 후, 보조) 병행. H1~H5는 A에서만 검정, A/B 비교는 탐색적 | 실제 배포 시 감염 시점을 알 수 없으므로, 감염 후 구간만 쓰면 1A.2절의 "실행 후 수 분 내 탐지" 정당화와 충돌 | 3.3절, 3.3.1절, Phase 3~4 실험량 증가 | 설정 B는 Phase 3~4까지만, Phase 6 미적용 |
| 2026-09-18 / 외부 검토 반영 ④ RF 기준값 | "n_estimators 기본 300" | **"본 연구 고정 기준값 300"으로 표현 변경** 및 선정 사유 명기 | scikit-learn 1.9.1의 라이브러리 기본값은 100이므로, "기본 300"은 오해를 유발 | 4.2절 | 숫자 300 자체는 유지 |
| 2026-09-18 / 외부 검토 반영 ⑤ MIA target | target model과 탐지 모델 A/B/C/D의 관계 미명시 | **5.0절 신설** — target은 A/B/C/D를 MIA 전용 분할로 재학습한 인스턴스. 하이퍼파라미터 재탐색 금지 | 미명시 시 Phase 3 탐지 성능표의 모델을 공격한 것으로 오해 가능. H3 검증에는 모델 4종 각각 공격이 필요 | 5.0절, Phase 4 MIA | Phase 3 확정 하이퍼파라미터 재사용 |
| 2026-09-18 / 외부 검토 반영 ⑦ H1 | H1을 가설로 두고 "축별 지표 간 상관관계 보고" | **1.1절 신설** — H1을 확증적 가설에서 제외. 상관계수·유의성 검정 미수행, 4종 병렬표 + 반례 존재 여부로 판정 | 모델 4종이므로 n=4 상관은 해석 불가. 시드 반복을 표본으로 환산하면 유사반복. H1은 부정 명제이므로 반례 하나로 판정 가능 | 1.1절, H1 가설행, 8장 보정 대상 | 다중비교 보정 대상에서 제외 |
| 2026-09-18 / 외부 검토 반영 ⑧ 특징중요도 | permutation importance 산출 데이터·지표·반복 미명시 | **6.4절 신설** — Validation / scoring=MCC / n_repeats=10. 교란 실험도 동일 Validation 집합. MDI·TreeSHAP 미사용 | H2는 중요도와 교란 반응의 상관을 보므로 동일 데이터·동일 특징 집합에서 산출되어야 성립. Test는 9장 보호 대상 | 6.4절, H2 가설행 | TreeSHAP은 모델 간 공정 비교를 해쳐 배제 |
| 2026-09-18 / 외부 검토 반영 ⑪ 불균형 | "Class Weight → Under → Over 순" (누적/택일 모호) | **4.3절 재작성 — 택일임을 명시**, 조건별 전환 규칙 및 "처리하지 않음" 행 추가 | 순차 누적으로 읽힐 수 있었음. Under와 Over 동시 적용은 상쇄·왜곡을 유발 | 4.3절 | CIC-MalMem-2022는 완전 균형이라 미적용 예상 |
| 2026-09-18 / 외부 검토 반영 ⑫ 중복제거 | "\|r\| > 0.95" (Pearson/Spearman 미명시, 제거 대상·시점 미정) | **Spearman(\|ρ\|>0.95), 컬럼 순서상 후자 제거, 분할 직후·스케일링 직전 수행**으로 구체화 | 발생률 기반 특징은 왜도·이상치가 커 Pearson이 부적합. 임의 선택 시 재현 불가 | 3.3절, 3.4절 | H2의 Spearman과 일관 |
| 2026-09-18 / 외부 검토 반영 ⑨ 스태킹 상관 | (신규 제안: 베이스 모델 예측 상관 측정 추가) | **추가하지 않음** | 다양성 지표와 앙상블 성능의 관계가 이론적으로 확립되지 않아(Kuncheva & Whitaker 2003) 측정해도 해석이 어려움. 실험 범위 유지 우선 | 변경 없음 | — |
| 2026-09-18 / 비용 가중 검토 | (해당 없음 — 암묵적 등가중 가정이 명시되지 않은 상태) | **1A.4절 신설**(비용 민감 평가 미수행을 명시적 한계로 선언) + **8장에 등가중 가정 선언 추가** | MCC·Youden's J·임계값 0.5에 오탐/미탐 등가중 가정이 공통으로 깔려 있으나 명시되지 않았음. 비용비를 정할 근거가 없어 비용 가중 도입 대신 가정을 명시하고 한계로 기록하는 방향 선택 | 8장, 1A.4절, 논문 한계점·향후과제 서술. 실험 절차 변경 없음 | 등가중 가정 유지, 비용 민감 평가 미수행 |
| 2026-09-18 / 이진분류 판정기준 문헌 검토 | 주평가지표 F1-score (8장) | **주지표를 MCC로 교체.** F1·Precision·Recall·PR-AUC·혼동행렬은 기술적 보고로 병기(가설검정 대상 아님). 8.1절 신설(혼동행렬 제시 규칙) | F1은 진음성(TN)을 계산에 포함하지 않아, 오탐률을 핵심 관심사로 삼는 본 연구의 의사결정 지표로 부적합(Chicco & Jurman 2020). Phase 1 착수 전이므로 사전등록 원칙 위반 아님 | 8장, 4.2절 BO 목적함수, 10.2절 허용한도, 12장 미수렴 기준, 13A절 스윕 지표, H1·H4 서술 | Chicco & Jurman(2020) BMC Genomics 21:6 / 반론: Holzmann & Klar |
| 2026-09-18 / BCCC 데이터셋 접근 승인 기한 도래 | 주실험 데이터셋으로 BCCC-MalMem-SnapLog-2025 또는 BCCC-Mal-NetMem-2025 사용 (11.1절 1순위) | **CIC-MalMem-2022로 자동 전환** (11.1절 2순위 적용) | 11.1절에 사전 정의된 기한(2026-09-18)까지 BCCC 측 승인 회신이 없었음. 실험 결과와 무관한 접근성 사유이며, 사전 조건문 실행에 해당 | 주실험 전체(Phase 1~6). 교차검증 데이터셋(DMBD 2025)은 변경 없음. 논문에 2022년 수집 데이터임을 명시하고 "최신 악성코드 동향 반영" 주장 금지 | CIC-MalMem-2022 (Carrier et al., ICISSP 2022), 58,596 레코드, VolMemLyzer 55개 특징 | (해당 없음 — 범위 유지 결정) | 편향-분산 분해(Domingos 2000)를 **이론 인용 및 결과 해석 근거로만 사용**, 실측은 수행하지 않음 | 0-1 손실에 직접 적용 가능한 분해 이론을 확보했으나, 실측 추가 시 사전등록 시점의 실험 범위를 벗어남. 범위 유지를 우선 | 논문 2장·결과 해석 서술. 실험 절차 변경 없음 | Domingos(2000) AAAI/ICML / mlxtend bias_variance_decomp 미사용 |

---

- v1.0~v1.1: 최초 프로토콜, 클래스불균형·중단기준 신설
- v1.2: 연구범위 축소, 그룹기반분할, MIA축제외규칙 폐기, 용어수정, 모델4종 세분화, H5 규칙 신설, DMBD공식분할 보존
- v1.3: (1) MIA 평가셋 독립 분할 재설계(옵션A/B 조건부 전환) (2) CI 비교를 "개별CI 중첩"에서 "차이의 CI"로 전면 수정 (3) 교란 유효성 기준을 검증가능/불가능으로 분리 (4) 4.2절 신설, 하이퍼파라미터·탐색범위 구체화 (5) 임계값 용도 분리 (6) H4 축별 재현판정 분리 (7) H5 "20%개선"을 무작위기준 초과분 대비로 정의 (8) H3 표현 하향("순수 구조효과"→"파이프라인 차이") (9) 14장 변경이력표 복원
- v1.4: 11.1절 신설 — 주실험 데이터셋 전환 규칙을 접근성 기반 사전 결정 형태로 명문화 (2026-09-18 기한, 미승인 시 CIC-MalMem-2022로 자동 전환). 대체 데이터셋의 프로토콜 적합성(family 라벨·샘플당 복수 덤프·다중 스냅샷 구조) 사전 확인 결과 및 최신성 한계 명시 의무 포함
- v1.5 (2026-09-15): IDS-ML 코드 분석 결과 반영 — (1) 4.2절 BO 항목에 BO-TPE(hyperopt) 알고리즘 명시 및 RF 탐색공간 구체화 (2) 4.2.1절 신설: 선행 구현 대비 의도적 상이점 3건(OOF 스태킹, BO 목적함수, 그룹 분할) 문서화 (3) 14장 변경이력에 당일 작업 3건 기록
- v1.6 (2026-09-15): 데이터 가공 파이프라인 확정 — (1) 3.2절 신설: 분석 단위를 "1회 실행 = 1 샘플"로 확정(스냅샷 개별 취급 금지), 표본 감소 대가와 한계 명시 의무 포함 (2) 3.3절 신설: 스냅샷 집계 방식을 감염시점 라벨 유무에 따른 조건부 자동결정 규칙으로 사전등록(통계요약 = 평균·최대·표준편차·기울기) (3) 3.4절 신설: 누수 방지 처리 순서 명문화(분할이 스케일링·불균형처리보다 선행) (4) 4.2.1절에 IDS-ML 전처리 순서 상이점 추가 (5) 13장 Phase 1 확정항목에 집계 관련 3건 추가
- v1.7 (2026-09-15): 1A절 신설 — (1) 본 연구가 침입 탐지가 아니라 "실행 중 악성 행위 탐지"임을 명시하고 IDS 용어 사용을 금지 (2) 관측 창(수 분)이 breakout time 29분·TTE 43분 등 실제 위협 시간 척도와 부합함을 근거로 정리, 7장 리소스 진단의 중요성과 연결 (3) "저활동 잠복형 악성코드는 탐지 대상에서 사실상 제외"를 구조적 한계로 명시하고 결과 해석·보고 의무 규정, 파일 이벤트율 특징의 판별력 제한 가능성을 Phase 4 확인 항목으로 연결
- v1.8 (2026-09-15): 근거 문헌 검증 결과 반영 — (1) 6.3절 레지스트리 난이도의 "권한 불필요" 항목을 X→O로 정정(HKCU Run 키는 사용자 권한만으로 조작 가능, Dieterich et al. ICISSP 2023 실험 근거). 분류 결과는 쉬움으로 불변, 근거 정확성만 개선. 각주로 HKCU/HKLM 구분 명시 (2) 3.1절에 특징 선택의 실증 근거 추가(Barr-Smith et al. IEEE S&P 2021, 3,180만 샘플 — 레지스트리 변경·정찰·지속성은 악성에서 유의하게 빈번, 프록시 실행은 정상·악성 공통이라 판별력 낮음) (3) 1A.3절에 은밀성-포착난이도 상관 근거 추가(APT의 LotL 사용률 26.26% vs 일반 9.41%) 및 샌드박스 수집 편향 명시
- v1.9 (2026-09-16): 13A절 신설 — 모델 복잡도 스윕을 **탐색적 부가 분석**으로 추가. (1) 확증적 가설(H1~H5)에 추가하지 않고 탐색적으로 명시 선언하여 8장 다중비교 보정 부담 회피 (2) Phase 6 경량화(처방)와 13A(관계 탐색)의 성격·발동조건·평가데이터를 표로 구분, H5와의 충돌 방지 (3) Validation만 사용하도록 제한하여 9장 Test 보호 원칙 유지 (4) 스윕 단계(RF 5단계·MLP 4단계)를 사전 고정, 결과를 보고 단계 조정 금지 (5) 우선순위 최하 및 생략 시 처리 규칙 명시. 13장 Phase 표에 6A 행 추가

- v2.0 (2026-09-18): **주실험 데이터셋 확정** — 11.1절 사전 규칙에 따라 기한(2026-09-18) 내 BCCC 승인 회신이 없어 CIC-MalMem-2022로 자동 전환. 교차검증 데이터셋(DMBD 2025)은 변경 없음. 14장 기록 완료

- v2.1 (2026-09-18): **주평가지표 확정** — (1) 8장 주지표를 F1→**MCC**로 교체, 사유 및 반론(Holzmann & Klar) 함께 명시 (2) 보고 지표 세트를 MCC·F1·Precision·Recall·PR-AUC + 혼동행렬로 확대하되 **기술적 보고로 규정하여 다중비교 보정 대상에서 제외** (3) 8.1절 신설 — 혼동행렬은 "Validation MCC 최고 모델"이라는 **사전 고정 규칙**으로 선정(사후 선택 방지), 시드 5회 **평균 행렬** 제시, 시드별 원본은 부록 (4) 임계값 조정 시 부트스트랩 CI 병기 및 불안정 시 0.5 회귀 규칙 추가 (5) MCC 교체에 따라 4.2절 BO 목적함수·10.2절 허용한도(절대 0.05)·12장 미수렴 기준(절대 0.20)·13A절 스윕 지표·H1 서술 연쇄 수정 (6) H4에 **PR-AUC는 유병률 의존이므로 데이터셋 간 비교 불가** 주의 추가

- v2.2 (2026-09-18): 비용 가중 관련 정리 — (1) 8장에 **등가중 가정의 명시적 선언** 추가(MCC·Youden's J·임계값 0.5에 공통으로 깔린 전제임을 밝히고, 4.3절 Class Weight는 불균형 보정이지 비용 가중이 아님을 구분) (2) **1A.4절 신설** — 비용 민감 평가 미수행을 명시적 한계로 선언. 비용비를 정할 근거 부재(조직별 상이, 경보 피로 문제)와 Youden's J 비용비의 유병률 의존성을 사유로 기록하고, 향후 연구 과제 및 실무 적용 시 재설정 필요성을 보고 의무로 규정

- v2.3 (2026-09-18): 외부 검토 의견 9건 반영 — (1) **10.4절 신설**: H5를 "선택적 vs 일괄"의 비열등성+우위 이중 검정으로 재구성, δ 사전 고정, 판정불가 규칙 (2) **MCC 개선 기준을 절대차(0.05)로 변경**, 상대 변화율 폐기 (3) **3.3.1절 신설**: 감염시점 정보에 대한 이중 설정(A 주/B 보조), H1~H5는 A에서만 검정 (4) RF 300을 "본 연구 고정값"으로 표현 정정 (라이브러리 기본값은 100) (5) **5.0절 신설**: MIA target model 정의 (6) **1.1절 신설**: H1을 확증적 가설에서 제외, 상관분석 미수행·반례 판정으로 전환 (7) **6.4절 신설**: permutation importance를 Validation/MCC/n_repeats=10으로 고정, MDI·TreeSHAP 배제 (8) 4.3절 불균형 처리를 택일 우선순위 체인으로 명확화 (9) 중복제거를 Spearman·결정론적 제거 규칙으로 구체화. 스태킹 베이스 상관 측정은 미추가 결정

- v2.4 (2026-09-18): 2차 외부 검토 9건 반영 — (1) **H5 비열등성 부등식을 지표 방향별로 분리**(수학적 오류 수정) (2) **5.4절 신설: AUC_adv = max(AUC, 1−AUC)** 도입 및 전 판정 규칙에 적용 (3) **"CI 폭 > 2δ" 판정불가 규칙 폐기**, ±δ 경계 교차 여부에 의한 3분 판정으로 대체(수학적 오류 수정) (4) **10.3절: Phase 6 출발 모델을 Model A로 고정** (5) **6.4절: H2 분석 단위를 최종 입력 특징 컬럼으로 고정**, 교란 민감도 S_i 부호 정의 (6) 본문의 BCCC 잔재를 CIC-MalMem-2022로 통일 (7) nested CV 용어를 "Train 내부 3-fold CV"로 정정 (8) MCC·Youden's J·0.5의 비용 관련 성격을 표로 분리 (9) 12장 B·C 규칙이 Phase 6 진입과 무관함을 명시

- v2.5 (2026-09-18): **Phase 1 데이터 구조 확인 결과 반영** — (1) **3.2절 재확정: 분석 단위를 "1 실행"에서 "1 메모리 덤프(1행)"로 변경**, 3.3·3.3.1절 폐기. 정상 샘플에 실행ID가 없어 악성만 집계하면 std=0이 클래스를 완벽 판별하는 치명적 누수가 발생하기 때문 (2) **3.2.1절 신설** — 정상 그룹 구조 복원 불가 검정 근거 기록 (3) **2.1절 비대칭 그룹 분할** — 악성은 실행+family, 정상은 행 단위 (4) **3.3.2절** 중복 제거를 원 특징 55개 기준으로 이관, 상수 3개 사전 제거 (5) 조건부 규칙 확정: 불균형 미적용·MIA 옵션 A·shadow model 1순위 (6) 6.2~6.3절 난이도 분류가 실제 특징과 불일치함을 확인, Phase 2 재작성 과제로 등록

- v2.6 (2026-09-18): **Phase 2 완료 — 주실험 데이터셋 교체 및 특징 확정** — (1) **11장 전면 재작성**: CIC-MalMem-2022에서 환경 지문 발견(깊이 1 트리 99.65%)으로 주실험을 **DMBD 2025**로 교체, CIC-MalMem은 11.3절 벤치마크 타당성 사례연구로 전환 (2) **H4를 코호트 간 재현(2017 vs 2024)으로 재정의** (3) **3.2절 분석 단위를 실행(experiment)으로 복원** — DMBD는 양쪽 클래스 모두 실행 ID 보유 (4) **3.3.2절 특징 14개 확정**, detonation·dur_after_detonation_s 제외, **발생률 정규화 폐기**(duration 중앙값 1.36초로 불안정) (5) **6.3절 난이도 분류 전면 재작성**(쉬움 9/어려움 4/제외 1), H2의 n=9 (6) **11.2.1절**: 공식 분할 보존하되 연도-라벨 교란 명시 + **8장 코호트별 분리 보고 의무** (7) 조건 재판정: MIA 옵션 B, Class Weight 적용 (8) 1A.2절 관측 창 서술 정정

- v2.7 (본 문서, 2026-09-19): **Phase 3 완료 — 모델 4종 구축** — (1) **BO를 GP-EI로 대체**(hyperopt 미설치), 탐색공간 불변 (2) **4.3절 Class Weight 판정 정정** — Train이 50:50이므로 미적용 (3) MLP를 sklearn 기본값 (100,) 미튜닝으로 확정, A↔D 대칭성 확보 (4) 스태킹 베이스를 HistGradientBoosting으로 교체 (5) max_features 상한 [1,12] 확정 (6) **H2 분석단위 n=9→7** — 중복 제거로 교란 대상 2개 탈락, 규칙은 사후 변경하지 않음 (7) **11.2.2절 신설** — Train(50%)↔Test(78.8%) 사전확률 이동 발견

---

**현재 상태**: Phase 0~3 완료. 데이터셋 확정으로 **Phase 1(데이터 구조 파악) 착수 가능**.

**Phase 3 착수 전 잔여 확정 항목**: 4.2절 BO 탐색공간의 `max_features` 상한(최종 특징 수 확정 후), 4.3절 2·3순위 전환 조건 충족 여부, 3.3.2절 중복 제거 실제 적용 결과.
