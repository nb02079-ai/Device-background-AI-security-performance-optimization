# 실험 프로토콜 확정 문서 v1.5

**연구 주제**: 호스트 행위 기반 악성코드 탐지 모델의 탐지 성능, 학습 데이터 멤버십 노출, 특징 교란 민감도 및 추론 비용을 공동 평가하고, 진단에 기반한 선택적 개선의 효과를 검증한다.

**문서 목적**: 실험 시작(Phase 1) 전 모든 방법론적 결정을 사전에 고정하여, 결과를 보고 사후적으로 유리한 조건을 선택하는 것을 방지한다.
**상태**: v1.5 (2026-09-15 갱신) — 2차 외부 검토 반영 + 데이터셋 전환 규칙 + IDS-ML 코드 분석 결과 반영

---

## 1. 연구 질문 및 가설

**핵심 연구 질문**: 탐지 성능이 높은 호스트 행위 기반 악성코드 탐지 모델이, 프라이버시·특징 교란 민감도·추론 비용 측면에서도 안전한가?

| 가설 | 내용 | 검증 방식 |
|---|---|---|
| H1 | 탐지 성능(F1·AUC)이 높다고 해서 프라이버시(MIA AUC)·강건성(교란 민감도)·자원 효율성(지연시간)이 함께 보장되지는 않는다 | 축별 지표 간 상관관계 보고 (인과·보장 주장은 하지 않음) |
| H2 | 조작 용이 특징(6.2절)에 대한 permutation importance가 높을수록, 해당 특징 교란 시 악성 확률 감소폭이 크다 | Importance 순위와 교란-확률변화 기울기의 Spearman 상관계수 |
| H3 | 지정된 학습 조건 하에서, 모델 구조(RF/MLP) 및 튜닝 방법(기본/BO/스태킹)에 따라 파이프라인 수준의 프라이버시·강건성 차이가 관찰된다 | 4.1절 4개 모델 변형 비교. "순수 구조 효과"라는 표현은 사용하지 않음 — 특정 학습 조건 하에서 관찰된 파이프라인 간 차이로 한정해 해석한다 |
| H4 | BCCC에서 관찰된 취약성 패턴이 DMBD에서 독립적으로 재현된다 | 축별로 개별 재현 판정 후 전체 요약 (1.1절) |
| H5 | 진단 결과에 기반한 선택적 개선은, 일괄 개선 대비 목표축 개선은 동등 이상이면서 부작용은 더 적다 | 10장 규칙 + 무개선/선택적/일괄 3자 비교. "동등 이상"은 차이의 신뢰구간 기반으로 판정(8장) |

### 1.1 H4 축별 재현 판정 기준

| 축 | 재현 판정 기준 |
|---|---|
| 프라이버시(MIA) | 두 데이터셋 모두에서 MIA AUC의 95% 신뢰구간이 0.5를 포함하지 않음 (방향 일치 포함) |
| 강건성(교란 민감도) | 두 데이터셋 모두에서 20% 조작 시 flip rate 증가폭의 신뢰구간이 0을 포함하지 않음 |
| 리소스 효율성 | 재현/미재현의 이분법 판정 대상이 아님. "무작위 기준"이라는 개념이 성립하지 않으므로, 두 데이터셋의 상대적 비용 순위만 기술적으로 비교·서술 |
| 종합 판정 | 프라이버시·강건성 2개 축의 판정 결과를 그대로 병기. "전체 재현 성공/실패"라는 단일 결론으로 뭉뚱그리지 않는다 |

---

## 2. 데이터 분할 및 누수 방지 (탐지 성능 평가용 — 미지 family 일반화)

### 2.1 분할 원칙
- 분할 단위: family 비중복 그룹 기반 분할 (미지 family 탐지 평가가 기본 설정)
- 분할 비율: Train 50% / Validation 20% / Test 30% (family 단위 배분)
- 표본 수 조정: family 수가 적으면 fold 수를 줄이거나 leave-one-group-out으로 전환하되, family 비중복 원칙은 항상 유지

> 이 분할은 "탐지 성능의 미지 family 일반화 평가"를 위한 것이며, 5장의 MIA 평가는 별도의 독립적인 분할 설계를 사용한다.

### 2.2 Test 고정과 시드 반복
Test는 최초 1회만 분할·고정. 5개 시드(42, 7, 123, 2024, 777)는 모델 초기화·학습·교란 탐색에만 적용하고 Test 재분할에는 적용하지 않는다.

### 2.3 전처리 누수 방지
결측치 처리·스케일링·특징선택·클래스 재표집은 Train 내부에서만 적합. 하이퍼파라미터 튜닝은 Train 내부 중첩 교차검증으로 수행하고 그 결과를 최종 성능으로 사용하지 않는다.

---

## 3. 공통 특징 확정 규칙

v1.2와 동일: 원본컬럼/이벤트-상태구분/집계대상/관찰시간창/계산식 5요소 문서화 필수, 0과 결측 구분, 우측절단 프로세스 플래그, 강제통합 금지, 공통특징 2개 이하 시 H4 범위 축소.

---

## 4. 모델 구성, 하이퍼파라미터, 탐색 범위

### 4.1 모델 4종

| 구분 | 모델 | 역할 |
|---|---|---|
| A | RandomForest (기본) | 주 배포 후보, 기준점 |
| B | RandomForest + 베이지안 최적화 (스태킹 없음) | 튜닝 효과만 분리 |
| C | RandomForest + 베이지안 최적화 + 스태킹 | 튜닝+구조결합 효과 |
| D | MLP | 모델 구조 대조군 |

### 4.2 탐색 범위 부록표 (Phase 1 데이터 규모 확인 후 최종 수치 확정, 아래는 기본값)

| 항목 | 내용 |
|---|---|
| RF (A) | n_estimators 기본 300 / 학습종료: scikit-learn 기본 수렴조건. 탐색 범위는 아래 BO 항목의 공간을 따름 |
| MLP (D) | 은닉층 1~2개, 유닛 수 [16, 128] / 조기종료: Validation loss 10 epoch 정체 시 중단 / 최대 200 epoch |
| BO (B, C) | **알고리즘: hyperopt의 BO-TPE(Tree-structured Parzen Estimator)** / 목적함수: **Train 내부 nested CV의 Validation F1** (Test 미사용) / 시행 30회 / 내부 그룹분할: Train 내 3-fold (family 비중복 유지) |
| BO 탐색공간 (RF 계열) | n_estimators [10, 200] / max_depth [5, 50] / max_features [1, 20] / min_samples_split [2, 11] / min_samples_leaf [1, 11] / criterion {gini, entropy} — IDS-ML(Yang et al.) 검증 범위를 채택. Phase 1에서 특징 수 확인 후 max_features 상한만 실제 특징 수에 맞게 조정 |
| 스태킹 (C) | 베이스모델: RF 3종(다른 시드) + Gradient Boosting 1종 / 메타모델: 로지스틱회귀 / OOF 분할: Train 내 5-fold |
| 개선후보-정규화+이산화 | 정규화 강도 3단계, 출력 이산화 소수점 1~2자리 |
| 개선후보-교란학습 | 6장 flip 성공 샘플을 원레이블 유지 증강, 비율 [10%, 50%] 탐색 |
| 개선후보-경량화 | RF 트리 수 [20, 100] / MLP 유닛 축소비율 [30%, 70%] |
| 일괄개선 비교군 | 순서: 경량화→정규화+이산화→교란학습 순 고정 적용 / 선택적 개선과 총 탐색예산 동일하게 맞춤 |

> 탐색 도중에는 이 범위를 넓히지 않는다.

### 4.2.1 선행 구현(IDS-ML) 대조 결과 및 의도적 상이점 (2026-09-15 기록)

IDS-ML 저장소(Western OC2 Lab, Yang et al.)의 세 노트북을 코드 수준에서 분석한 결과, 본 프로토콜과 **의도적으로 다르게 설계한 지점** 3가지를 확인하였다. 논문 방법론 서술 시 "선행 연구의 일반적 관행 대비 개선점"으로 명시한다.

| 항목 | IDS-ML 구현 | 본 프로토콜 | 상이 사유 |
|---|---|---|---|
| 스태킹 메타모델 학습 데이터 | 베이스 모델이 **자신의 학습 데이터(X_train)를 그대로 예측한 결과** 사용 (`Tree-based_IDS_GlobeCom19` L320-327) | OOF(Out-Of-Fold) 5-fold 예측 사용 | in-sample 예측은 베이스 모델의 과적합된 출력이므로, 메타모델이 "베이스 모델은 항상 옳다"는 비현실적 패턴을 학습하게 됨 (데이터 누수) |
| BO 목적함수 | **Test set 점수**를 최대화 (`MTH_IDS_IoTJ` L453-455) | Train 내부 nested CV의 Validation F1 | Test로 튜닝하면 Test가 더 이상 미관측 데이터가 아니게 되어 최종 성능이 과대평가됨 |
| 데이터 분할 | 단순 층화 무작위 2분할 (`train_test_split(stratify=y)`) | family 비중복 그룹 기반 3분할 | 동일 family 변종이 train/test에 혼재하면 미지 family 일반화 성능이 과대평가됨 |

**채택한 부분**: BO-TPE 알고리즘(hyperopt) 및 RF 하이퍼파라미터 탐색 공간(4.2절 반영). 스태킹 베이스 모델 구성(DT/RF/ET/XGBoost 4종)은 본 프로토콜(RF 3종+GB 1종)보다 다양성이 높아 Phase 3에서 재검토 여지 있음 — 단 변경 시 14장에 기록.

### 4.3 클래스 불균형 처리
v1.2와 동일: Class Weight → Under-sampling → Over-sampling 순, Train 내부에서만 수행.

---

## 5. 멤버십 추론 공격(MIA) 프로토콜 — 독립 분할 설계

> "MIA는 정의상 Test를 사용하지 않는다"는 v1.2의 표현은 부정확했다. Member/Non-member 구분은 학습 포함 여부일 뿐이며, Test를 non-member로 쓰지 않는 것은 연구 설계상의 선택이다. 이번 버전은 그 선택을 명시적으로 재설계한다.

### 5.1 기본안 (옵션 A)
2장의 "미지 family 탐지" 분할과 별도로, MIA만을 위한 독립적인 member/non-member 셋을 구성한다.
- 적용 조건: family 내 서로 다른 개별 샘플(변종·실행 인스턴스)이 2개 이상 존재하는 family가 전체 family의 30% 이상일 때 적용 가능
- 구성 방법: 그런 family에서 한 개별 샘플은 target model 학습(Train)에 포함, 같은 family의 다른 개별 샘플은 학습에서 제외해 non-member 후보로 사용 → family 분포가 실제로 일치하면서 차이가 "학습 포함 여부"로만 귀결됨
- 역할별 데이터 3분리 (재사용 금지):
  - 공격 학습·임계값 보정: member/non-member 각 50% 무작위 추출
  - 개선안(프라이버시 방어) 선택 근거: 나머지 50%의 절반
  - 최종 감사(Phase 6 전용): 남은 절반. ID는 Phase 4 시점에 고정, 이후 어떤 단계에서도 재열람하지 않는다

### 5.2 대체안 (옵션 B, 조건부 자동 전환)
Phase 1에서 옵션 A 적용조건(family당 복수샘플 30%+)을 충족하지 못하면 자동 전환.
- Member = Train, Non-member = Validation (2장 분할 그대로)
- 이 경우 "family가 교란된(confounded) MIA 결과"임을 명시하고, "실제 멤버십 신호를 보장한다"는 표현은 사용하지 않는다. 대신 "family 신규성과 멤버십 신호가 혼재될 수 있으며, 이는 데이터 규모상 불가피한 한계"라고 기술한다.

### 5.3 공격 방법 우선순위 체인

| 순위 | 방법 | 적용 조건 |
|---|---|---|
| 1순위 | Shadow-model 기반 MIA | 클래스별 고유 그룹 수 ≥ 100 |
| 2순위 | Confidence-threshold 기반 MIA | 1순위 미충족 시 자동 전환 |

- 축 제외 없음: 공격 성능이 낮게 나와도(AUC≈0.5) 축 자체를 제외하지 않는다. 보고 문구: "사전 정의된 공격 조건 하에서 멤버십 신호가 검출되지 않았다. 이는 프라이버시가 보장됨을 의미하지 않는다."
- 평가지표: Accuracy, Precision, Recall, F1, ROC-AUC, TPR@1%FPR

---

## 6. 특징 교란 민감도 실험

> 용어 통일: 이 실험 전체를 "특징 교란 민감도 실험"으로 부르며, "회피 공격", "회피 성공률"은 문서 전체에서 사용하지 않는다. 결과 지표명은 "특징 공간 제약을 만족하는 교란에 대한 예측 전환율(flip rate)"로 통일.

### 6.1 측정 방식
- 분류 임계값 용도 분리: flip rate는 항상 고정 임계값 0.5 기준 (일관된 스트레스테스트 기준점 확보). 8장의 Validation 기반 가변 임계값은 Phase 3 탐지성능 지표 산출에만 적용하며 본 실험에는 적용하지 않는다.
- 이중 지표: (1) 고정 임계값 0.5 기준 flip rate, (2) 악성 확률 감소폭(연속값)
- 조작 단계: 5/10/15/20/30% 감소
- 분모: 원래 모델이 정확히 악성으로 분류한 표본 수만 포함 (원래 미탐 표본 제외)

### 6.2 유효성 기준 — 검증 가능/불가능 분리

본 실험 환경에는 악성코드 실행 샌드박스가 없으므로 "실제 악성 기능 유지 여부"는 검증 불가능하다. 유효성 판정은 검증 가능한 조건만으로 한정한다.

**검증 가능 (유효성 판정에 사용)**: 특징값 비음수 제약, 데이터 관찰 범위(Train 내 min/max) 초과 여부, 특징 간 정의상 제약 위반 여부

**검증 불가 (가정으로만 명시)**: 실제 악성 기능의 유지 여부 — 아래 난이도표의 "기능유지" 열은 검증된 사실이 아니라 전문가 판단에 근거한 가정임을 각주로 명시

### 6.3 조작 난이도 분류 (기준 3개 중 2개 이상 → "쉬움")

| 특징 | 기능유지(가정)* | 권한불필요 | 반복가능 | 분류 |
|---|---|---|---|---|
| 네트워크 연결률 | O | O | O | 쉬움 |
| 파일 이벤트율 | O | O | O | 쉬움 |
| 레지스트리 이벤트율 | O | X | O | 쉬움 |
| 프로세스 생성률 | X | X | X | 어려움 |
| 모듈 로드율 | X | X | X | 어려움 |

*기능유지 열은 실행 기반 검증값이 아니라 전문가 판단에 근거한 가정 (6.2절 참고)

---

## 7. 리소스 벤치마크 조건

v1.2와 동일: 전처리/추론 비용 분리, events-per-batch = 집계된 특징벡터 수, tracemalloc + psutil RSS 병행, 워밍업·단일스레드·모델적재 고정, 10/100/1,000 events/min 3단계, 20회 반복.

---

## 8. 통계 처리

- 비교 방법: 모델·개선 전후 비교는 동일 평가 그룹에 대한 대응 비교로 수행하고, **지표 차이 자체의 95% 신뢰구간**을 산출한다. **개별 신뢰구간의 중첩 여부는 유의성 판정에 사용하지 않는다.**
- 주평가지표: 탐지 성능은 F1-score. 분류 임계값은 6장에서는 항상 0.5, Phase 3 성능 산출에서는 Validation 기준 Youden's J로 조정 가능(조정 시 사유 기록).
- 다중비교 보정: Benjamini-Hochberg 적용
- H5 "20% 개선"의 정의: MIA AUC는 **무작위 기준(0.5) 대비 초과분의 상대적 감소**로 정의한다. 예: 베이스라인 AUC 0.71(초과분 0.21) → 개선 후 초과분 0.168 이하(AUC 0.668 이하)면 "20% 개선"으로 판정 (원시 AUC의 20% 감소가 아님)
- 소표본: 신뢰구간 계산 기준을 완화하지 않는다. 결과를 "탐색적 결과"로 라벨링만 하고 계산 방식은 동일하게 엄격히 적용
- 부트스트랩: 재표집 단위는 독립 단위(바이너리/실행 그룹). 2,000회, 95% percentile CI

---

## 9. Phase 6 Test Set 및 MIA 평가셋 보호 원칙

- 탐지 성능: Phase 4 진단·Phase 6 개선안 선택은 Validation, 최종 평가는 Test
- MIA: 5.1절의 3분리(공격학습·개선안선택·최종감사)를 그대로 적용. 최종감사 ID는 Phase 4 시점에 고정, 이후 재열람하지 않는다

---

## 10. H5 선택적 개선 의사결정 규칙

### 10.1 진단축별 개선 후보 및 조건

| 진단 축 | 개선 후보 | 적용 조건(Validation) |
|---|---|---|
| 프라이버시 | 정규화+출력이산화 | MIA AUC 95% CI 하한 > 0.5 |
| 강건성 | 교란학습 | 20% 조작 시 flip rate 증가폭 CI 하한 > 0 |
| 리소스 | 경량화 | 샘플당 지연시간 > 1ms |

### 10.2 판단 기준
- 개선 성공 판단: 해당 축 핵심지표의 개선 전후 차이 95% CI가 "20% 개선 기준"을 포함하고 0을 포함하지 않을 때
- 허용 한도: 탐지 F1 저하 5%p, 오탐률 상승 10%p, 지연시간 50% 증가 중 하나라도 차이-CI 상한 초과 시 기각
- 타이브레이킹: (1) 목표축 개선폭 큰 순 → (2) 부작용 적은 순 → (3) 구현 복잡도 낮은 순
- 개선 실패 시: 미적용 및 실패 사실 그대로 보고

### 10.3 비교군 (Test에서 3자 비교)
1. 무개선 기준선  2. 선택적 개선  3. 일괄개선(4.2절 순서·예산 고정)

---

## 11. 데이터셋 확정 및 교차검증 설계

### 11.1 주실험 데이터셋 전환 규칙 (접근성 기반 사전 결정)

본 연구의 주실험 데이터셋은 접근 승인 여부에 따라 아래 우선순위대로 **자동 결정**한다. 이 전환은 실험 결과와 무관한 순수 접근성 사유이며, 결정 시점과 사유를 14장 변경이력에 기록한다.

| 순위 | 데이터셋 | 적용 조건 |
|---|---|---|
| 1순위 | BCCC-MalMem-SnapLog-2025 또는 BCCC-Mal-NetMem-2025 | **2026년 9월 18일까지** 접근 승인 완료 시 (둘 다 승인되면 SnapLog 우선, 단 용량이 실행 가능한 범위일 것) |
| 2순위 | CIC-MalMem-2022 | 위 기한까지 BCCC 승인이 이루어지지 않을 경우 자동 전환 |

**2순위 전환 시 프로토콜 적합성** (사전 확인 완료):
- family 라벨(subtype 15종: Zeus, Emotet, Gator, Pysa 등)이 레코드별로 존재 → 2.1절 family 비중복 분할 수행 가능
- 악성코드 샘플당 10개 메모리 덤프 존재 → 5.1절 MIA 옵션 A 적용조건(family당 복수 샘플) 충족 가능성 높음. 단 Phase 1에서 실제 확인 후 5.2절 규칙에 따라 옵션 A/B 확정
- 15초 간격 다중 스냅샷 구조 → 3장 공통특징 설계 및 스냅샷 집계 방식 논의를 그대로 적용 가능
- **한계 명시 의무**: 2022년 데이터이므로 "최신 악성코드 동향 반영"이라는 주장은 하지 않으며, 데이터 수집 시점을 논문에 명시한다

교차검증 데이터셋은 어느 경우에도 DMBD 2025를 유지한다.

### 11.2 DMBD 교차검증 설계

독립 재현 원칙 유지(모델 전이 아님). 공식 train/test 분할(2024 신종 악성코드 홀드아웃) 보존, 본 연구의 Validation은 공식 train을 재분할하여 확보(family 비중복 원칙 적용). 재현 판정 기준은 1.1절.

---

## 12. 중단·대체·제외 기준

| 조건 | 처리 방식 |
|---|---|
| 특정 특징이 공통 정의 불가/결측 30%+/의미 상이 | 공통특징에서 제외 또는 데이터셋별 개별 특징 유지 |
| 교란 표본이 6.2절 검증가능 기준을 벗어남 | "무효 변형"으로 집계, 성공률 계산 제외 |
| B·C 모델과 A의 결과 차이의 CI가 0을 포함 | Phase 6 적용 생략, "유의미한 차이를 발견하지 못함"으로 기술 |
| BCCC 다중 스냅샷 집계방식별 결과 차이의 CI가 0을 불포함 | 집계방식별 결과 병기, 대표값 임의선택 금지 |
| 모델 미수렴/과적합(Train-Val F1 차이 > 20%p) | 4.2절 범위 내 재조정만 허용, 범위 밖이면 해당 축 제외+사유기록 |
| 리소스 측정환경 재현 불가 | 상대비율 중심 보고, 측정환경 사양 명시 |
| DMBD 변환 후 그룹 수 < 30 | 탐색적 결과로 한정, 통계기준은 동일하게 유지 |
| MIA 공격자 모델 불안정(시드간 AUC 변동 > 0.1) | 5.3절 2순위로 자동전환, 축 제외 없음 |
| MIA 옵션A 적용조건(family당 복수샘플 30%+) 미충족 | 5.2절 옵션B로 자동 전환, confound 명시 문구 사용 |

---

## 13. 전체 Phase 흐름 및 산출물

| Phase | 내용 | 주요 산출물 | Phase 1 이후 확정 필요 항목 |
|---|---|---|---|
| 0 | 프로토콜 확정 | 본 문서 | - |
| 1 | 데이터 구조 파악 | 컬럼·라벨분포·결측·고유샘플수·family당 샘플수 | MIA 옵션 A/B 선택, 그룹기반 fold 수, 클래스불균형 처리단계, 4.2절 탐색범위 최종수치 |
| 2 | 공통 특징 확정 | Feature Set v1.0 | - |
| 3 | 4개 모델 구축 | 베이스라인 성능표 | - |
| 4 | 3대 진단 + 특징중요도 | MIA·교란민감도·리소스 결과 | - |
| 5 | DMBD 독립 재현 | H4 축별 재현판정 | - |
| 6 | 선택적 개선 | 3자 비교 | - |
| 7 | 논문 작성 | 최종 원고 | - |

---

## 14. 변경 이력 및 예외 기록 규칙

| 발생 조건 | 원래 계획 | 실제 처리 | 변경 이유 | 영향 범위 | 재현 정보(seed/버전/파라미터) |
|---|---|---|---|---|---|
| 2026-09-15 / IDS-ML 환경 구축 | 로컬에서 IDS-ML 노트북 실행하여 파이프라인 구조 파악 | 실행 포기 후 `.py` 변환하여 코드 분석으로 전환 | 2019년 코드와 최신 라이브러리 간 버전 충돌 4건(pandas dtype 2건, imblearn API 2건) 발생. 목적이 "실행 결과"가 아닌 "구조 파악"이므로 코드 읽기로 충분 | Phase 0 준비 단계에 한정, 실험 결과에 영향 없음 | Python 3.13.15, scikit-learn 1.9.1, Windows 11 / nbconvert 사용 |
| 2026-09-15 / IDS-ML 코드 분석 | 4.2절 BO 설정을 "베이지안 최적화, 30회"로만 기술 | BO-TPE(hyperopt) 알고리즘 명시 + RF 탐색공간 구체화 (4.2절) | IDS-ML에서 검증된 탐색 범위를 채택하여 임의 설정을 줄임 | 4.2절, Phase 3 이후 모델 B·C | hyperopt TPE, max_evals=30 |
| 2026-09-15 / IDS-ML 코드 분석 | (해당 없음 — 신규 기록) | 4.2.1절 신설: 선행 구현 대비 의도적 상이점 3건 문서화 | 선행 연구에서 데이터 누수 패턴 2건(스태킹 in-sample 예측, BO 목적함수에 Test 사용) 발견. 본 프로토콜이 이를 이미 방지하고 있음을 확인하고 근거로 기록 | 4.2.1절, 논문 방법론 서술 | Tree-based_IDS_GlobeCom19.py L320-327, MTH_IDS_IoTJ.py L453-455 |

---

## 버전 이력

- v1.0~v1.1: 최초 프로토콜, 클래스불균형·중단기준 신설
- v1.2: 연구범위 축소, 그룹기반분할, MIA축제외규칙 폐기, 용어수정, 모델4종 세분화, H5 규칙 신설, DMBD공식분할 보존
- v1.3: (1) MIA 평가셋 독립 분할 재설계(옵션A/B 조건부 전환) (2) CI 비교를 "개별CI 중첩"에서 "차이의 CI"로 전면 수정 (3) 교란 유효성 기준을 검증가능/불가능으로 분리 (4) 4.2절 신설, 하이퍼파라미터·탐색범위 구체화 (5) 임계값 용도 분리 (6) H4 축별 재현판정 분리 (7) H5 "20%개선"을 무작위기준 초과분 대비로 정의 (8) H3 표현 하향("순수 구조효과"→"파이프라인 차이") (9) 14장 변경이력표 복원
- v1.5 (본 문서, 2026-09-15): IDS-ML 코드 분석 결과 반영 — (1) 4.2절 BO 항목에 BO-TPE(hyperopt) 알고리즘 명시 및 RF 탐색공간 구체화 (2) 4.2.1절 신설: 선행 구현 대비 의도적 상이점 3건(OOF 스태킹, BO 목적함수, 그룹 분할) 문서화 (3) 14장 변경이력에 당일 작업 3건 기록
- v1.4: 11.1절 신설 — 주실험 데이터셋 전환 규칙을 접근성 기반 사전 결정 형태로 명문화 (2026-09-18 기한, 미승인 시 CIC-MalMem-2022로 자동 전환). 대체 데이터셋의 프로토콜 적합성(family 라벨·샘플당 복수 덤프·다중 스냅샷 구조) 사전 확인 결과 및 최신성 한계 명시 의무 포함

**다음 단계**: 이 버전을 최종 설계안으로 삼아 Phase 1로 진행 권장. 13장의 "Phase 1 이후 확정 필요 항목"은 실제 데이터 없이는 더 정교화할 수 없다.
