# 실험 프로토콜 확정 문서 v1.2

**연구 주제(수정)**: 호스트 행위 기반 악성코드 탐지 모델의 탐지 성능, 학습 데이터 멤버십 노출, 특징 교란 민감도 및 추론 비용을 공동 평가하고, 진단에 기반한 선택적 개선의 효과를 검증한다.

> v1.1까지 "백그라운드 보안 에이전트" 전체를 평가 대상처럼 서술했으나, 본 연구는 에이전트의 권한 관리·업데이트 보안·로그 보호·상시 실행 환경까지는 평가하지 않는다. 따라서 주장 범위를 **"호스트 행위 기반 악성코드 탐지 모델"**로 한정한다.

**문서 목적**: 실험 시작(Phase 1) 전 모든 방법론적 결정을 사전에 고정하여, 결과를 보고 사후적으로 유리한 조건을 선택하는 것을 방지한다.
**상태**: v1.2 — 외부 검토 반영본
**버전 이력은 문서 최하단 참고**

---

## 1. 연구 질문 및 가설

**핵심 연구 질문**: 탐지 성능이 높은 호스트 행위 기반 악성코드 탐지 모델이, 프라이버시·특징 교란 민감도·추론 비용 측면에서도 안전한가?

> 아래 가설들의 **검증 대상은 "예측 방향의 진위"**이지, "결과가 반드시 가설을 지지해야 함"이 아니다. 즉 H2처럼 특정 방향을 명시한 가설도, 실제 실험에서 반대 방향이나 무관계로 나오면 그 결과를 그대로 보고한다 ("방향을 미리 가정하지 않는다"는 것은 "결과를 예단하지 않는다"는 뜻이며, 가설 문장 자체가 방향을 갖는 것과는 별개이다).

| 가설 | 내용 | 검증 방식 (수정) |
|---|---|---|
| H1 | 탐지 성능(F1·AUC 등)이 높다고 해서 프라이버시(MIA AUC)·강건성(교란 민감도)·자원 효율성(지연시간)이 함께 보장되지는 않는다 | 축별 지표를 정의역과 함께 명시하고, 네 축 지표 간 상관관계를 보고 (인과·보장 주장은 하지 않음) |
| H2 | 공격자가 조작하기 쉬운 특징(6.2절 분류)에 대한 모델의 permutation importance가 높을수록, 해당 특징 교란 시 악성 확률 감소폭이 크다 | 특징별 importance 순위와 특징별 교란-확률변화 기울기의 상관계수(Spearman) 산출 |
| H3 | 모델 구조(RF/MLP)뿐 아니라 동일 모델의 튜닝 방법(기본/BO/스태킹)에 따라서도 프라이버시·강건성 특성이 달라진다 | 4.1절 4개 모델 변형 간 비교 + RF 계열 내부의 BO-only/스태킹 단계적 비교로 구조 효과와 튜닝 효과를 분리 |
| H4 | BCCC에서 관찰된 취약성 패턴(방향·상대적 크기)이 DMBD에서 독립적으로 재현된다 | "재현 성공"의 기준을 사전 정의: 두 데이터셋에서 방향이 일치하고, 최소 하나의 축에서 통계적으로 유의미한 취약성(무작위 기준 대비)이 공통 관찰되면 재현으로 판정. 불일치 시 실패로 명시하고 원인 논의 |
| H5 | 진단 결과에 기반한 선택적 개선은, 모든 개선을 무조건 적용하는 일괄 정책 대비 동등하거나 더 나은 목표축 개선을 더 적은 부작용(다른 축의 성능 저하)으로 달성한다 | 2절(H5 의사결정 규칙) 및 무개선 기준선 + 일괄개선 비교군과 3자 비교 |

---

## 2. 데이터 분할 및 누수 방지

### 2.1 분할 원칙
- **분할 단위**: 그룹(동일 바이너리/실행 단위) 기반 분할을 원칙으로 한다. **표본 수와 무관하게 이 원칙은 항상 유지**한다 (v1.1의 "표본 100개 미만 시 일반 층화 5-fold 전환" 규칙은 그룹 비중복을 깨뜨리므로 폐기).
- **표본 수에 따른 조정**: 그룹 수가 적을 경우, fold 수를 줄이거나(예: 3-fold) leave-one-group-out으로 전환하되, 클래스 비율은 가능한 범위에서 층화한다. 그룹 기반 원칙은 항상 유지.
- **평가 질문 구분**: "이미 알려진 family의 새로운 개체 탐지"(같은 family가 train/test에 있어도 됨, 단 정확히 동일한 실행·스냅샷은 불가)와 "미지 family 탐지"(family 자체를 비중복 분할)는 서로 다른 연구 질문이다. 본 연구는 **미지 family 탐지를 기본 설정**으로 하며, family 단위 비중복을 유지한다.
- **분할 비율**: Train 50% / Validation 20% / Test 30% (그룹 단위로 배분)

### 2.2 Test 고정과 시드 반복의 관계 (v1.1 모순 수정)
- **Test는 최초 1회만 분할·고정**하며, 이후 어떤 이유로도 재구성하지 않는다.
- 8장에서 정의한 5개 시드(42, 7, 123, 2024, 777)는 **Test 재분할에는 적용하지 않고**, 아래에만 적용한다: (1) 모델 가중치 초기화, (2) Train 내부 학습 과정의 확률적 요소, (3) 교란 민감도 실험의 무작위 방향 탐색.
- Train/Validation 내부 분할 자체의 변동성을 별도로 평가하고 싶다면, 이는 위와 별개로 "5개의 서로 다른 Train/Validation 분할을 사전에 모두 확정해두고 전부 보고"하는 방식으로 명시적으로 설계해야 하며, 본 연구에서는 기본적으로 수행하지 않는다 (Phase 1에서 데이터 규모를 보고 필요성 재논의).

### 2.3 전처리 누수 방지
- 결측치 처리, 스케일링(MLP용), 특징 선택, 클래스 재표집(4.1절)은 **모두 Train 구간 내부에서만 적합(fit)**하고, Validation/Test에는 Train에서 학습된 변환만 적용한다.
- 하이퍼파라미터 튜닝(베이지안 최적화)은 Train 내부의 **중첩 교차검증(nested CV)**으로 수행하고, 그 결과를 최종 성능으로 사용하지 않는다. 최종 성능은 반드시 별도 Validation/Test로 평가한다.

---

## 3. 공통 특징 확정 규칙

- **필수 문서화 요건**: 최종 채택되는 특징마다 아래 5가지를 표로 기록해야 하며, 이 표 없이는 어떤 특징도 "공통 특징"으로 확정할 수 없다.
  1. 원본 컬럼명 (BCCC/DMBD 각각)
  2. 이벤트 기반인지 상태(스냅샷) 기반인지 구분
  3. 집계 대상 (예: PID별, 세션별)
  4. 관찰 시간 창의 정의
  5. 최종 계산식

- **이벤트 없음(0)과 결측(missing)의 구분**: 관찰은 되었으나 해당 이벤트가 발생하지 않은 경우(0)와,애초에 수집이 실패하거나 로그가 없는 경우(missing)를 별개 값으로 구분하여 저장한다. missing을 0으로 대체하지 않는다.
- **미완료 프로세스의 지속시간 처리**: 관찰 종료 시점까지 종료되지 않은 프로세스는 "우측 절단(right-censored)"으로 표시하고, 별도 플래그 컬럼을 둔다. 단순 삭제하거나 관찰 종료 시점을 종료 시점으로 대체하지 않는다.
- **강제 통합 금지**: 두 데이터셋에서 측정 의미 자체가 다른 경우(예: "스냅샷 내 프로세스 수"와 "분당 신규 프로세스 생성 수") 이를 같은 특징으로 묶지 않는다. 의미가 다르면 별도 특징으로 유지하거나, 한쪽 데이터셋에서만 사용 가능한 특징으로 명시한다.
- **공통 특징 부족 시 처리**: Phase 1~2 검토 후 공통 특징이 2개 이하로 확인되면, H4(교차검증)의 범위를 "완전한 재현 검증"에서 "제한적 정성 비교"로 축소하고 그 사유를 명시한다.

---

## 4. 모델 구성 및 하이퍼파라미터 규칙

### 4.1 모델 4종 (H3 검증을 위해 세분화)

| 구분 | 모델 | 역할 |
|---|---|---|
| A | RandomForest (기본 설정) | 주 배포 후보, 구조 비교의 기준점 |
| B | RandomForest + 베이지안 최적화 (스태킹 없음) | **튜닝 효과만** 분리 관찰 |
| C | RandomForest + 베이지안 최적화 + 스태킹 앙상블 | 튜닝+구조결합(스태킹) 효과, IDS-ML 파이프라인 참고 재구현 |
| D | MLP | 모델 구조(트리 vs 신경망) 대조군 |

- **H3 분해 로직**: A→B 차이 = 튜닝 효과, B→C 차이 = 스태킹(구조결합) 효과, A/B/C→D 차이 = 근본적 모델 구조 효과. 이렇게 세 비교를 분리해서 보고한다.
- **스태킹 메타모델 학습 방식**: 베이스 모델의 교차검증 예측(out-of-fold prediction)을 메타모델 학습에 사용한다 (Train 데이터에 대한 in-sample 예측을 그대로 쓰지 않음 — 이렇게 하면 메타모델이 베이스 모델의 과적합을 학습하는 것을 방지).
- **실험 범위**: A·D(RF기본·MLP)는 Phase 3~6 전체 파이프라인 수행. B·C(BO/스태킹)는 Phase 3~4까지 필수 수행. Phase 6 적용 여부는 Phase 4 결과에 따라 판단(10장 기준).
- **랜덤 시드**: 2.2절 기준에 따름.

### 4.2 클래스 불균형 처리 우선순위

Phase 1에서 실제 정상/악성 비율을 확인한 뒤, 불균형이 확인되면(소수 클래스 비율 40% 미만 기준) 아래 우선순위를 순서대로 적용하며, **Train 구간 내부에서만** 수행한다 (분할 전 재표집 금지).

| 순위 | 방법 | 전환 조건 |
|---|---|---|
| 1순위 | Class Weight 조정 | 기본 적용 |
| 2순위 | Under-sampling | 1순위 후에도 소수 클래스 재현율 < 0.5 |
| 3순위 | Over-sampling (SMOTE 등) | 소수 클래스 표본이 30개 미만으로 극히 적은 경우 |

---

## 5. 멤버십 추론 공격(MIA) 프로토콜

### 5.1 공격 설계 명세 (필수 사전 정의)
- **공격자에게 공개되는 정보**: 예측 확률(모델 파라미터·구조는 비공개, 블랙박스 가정)
- **데이터 역할 분리**:
  - Shadow model 학습: Train과 분포가 유사한 별도 재샘플링 데이터 (1순위 방법 적용 시)
  - 공격 임계값 선택: Validation 일부
  - 최종 공격 평가: Train(member) vs Validation(non-member) — **Test는 MIA 평가에 사용하지 않는다** (Test 순수성은 탐지 성능 평가 전용으로 보존)
- **Member/Non-member 매칭**: 클래스 비율·family 분포·수집 시기가 유사하도록 매칭하여, 공격 성능이 이런 교란 요인이 아니라 실제 멤버십 신호에서 나온 것임을 보장한다.
- **평가 표본 수·비율**: Member:Non-member = 1:1로 맞추고, 각각 최소 30개 이상 확보되지 않으면 결과를 "탐색적"으로 표시(8장).

### 5.2 우선순위 체인
| 순위 | 방법 | 적용 조건 |
|---|---|---|
| 1순위 | Shadow-model 기반 MIA (Shokri et al. 2017) | 클래스별 고유 그룹 수 ≥ 100 |
| 2순위 | Confidence-threshold 기반 MIA (Yeom et al. 2018) | 1순위 조건 미충족 시 자동 전환 |

> **v1.1 수정**: "공격자 모델이 불안정하면 3순위(해당 진단 축 제외)로 전환" 규칙을 **폐기**한다. AUC가 0.5 근처로 나오는 것은 "구현 실패"와 "실제 유출 신호 부재"를 구분할 수 없으므로, 어떤 경우에도 축 자체를 제외하지 않는다. 대신 보고 문구를 다음으로 고정한다: **"사전 정의된 공격 조건 및 공격 강도 하에서 멤버십 신호가 검출되지 않았다. 이는 프라이버시가 보장됨을 의미하지 않는다."**
- **평가지표**: Attack Accuracy, Precision, Recall, F1, ROC-AUC, 및 가능하면 낮은 오탐률(예: FPR 1%)에서의 True Positive Rate(TPR@low-FPR)도 함께 보고한다 (평균 지표로 가려지는 위험 포착).

---

## 6. 특징 교란 민감도 실험 (v1.1 "회피 공격"에서 용어 수정)

> **범위 한계 명시**: 본 실험 환경은 실제 악성코드 바이너리를 격리 실행할 수 있는 샌드박스를 갖추고 있지 않다. 따라서 여기서 측정하는 것은 **"특징 공간에서의 입력 교란에 대한 모델 출력 민감도"**이며, "실행 가능한 프로그램 변형을 통한 실제 회피 공격 성공률"이 아니다. 이 구분을 논문 전체에서 일관되게 유지하고, 실행 기반 검증의 부재를 명시적 한계점으로 기술한다.

### 6.1 측정 방식
- **이중 지표 보고**: (1) 고정 임계값(0.5)에서의 교란 후 오분류율(fixed-threshold flip rate), (2) 악성 확률 자체의 감소폭(연속값) — 이 둘을 함께 보고하여 H2와 6장 방법의 정합성을 확보한다.
- **조작 비율 단계**: 5% / 10% / 15% / 20% / 30% 감소, 단계별 dose-response 형태로 보고
- **분모 정의**: 회피 성공률의 분모는 "원래 모델이 정확히 악성으로 분류한 표본 수"로 한정한다 (원래부터 미탐(false negative)이었던 표본은 분자·분모 모두에서 제외).
- **무효 변형 처리**: 교란 결과가 6.2절 유효성 기준을 벗어나면 해당 표본은 "무효 변형"으로 별도 집계하고, 성공률 계산에서 제외한다.

### 6.2 조작 난이도 분류 기준 및 대상 (v1.1 오류 수정)
기준 3개 중 **2개 이상 충족 시 "쉬움"**으로 분류한다 (일관 적용).

| 특징 | 기준1(기능유지) | 기준2(권한불필요) | 기준3(반복가능) | 분류 |
|---|---|---|---|---|
| 네트워크 연결률 | O | O | O | 쉬움 |
| 파일 이벤트율 | O | O | O | 쉬움 |
| 레지스트리 이벤트율 | O | X | O | **쉬움** (2/3 충족, v1.1의 "중간" 오분류 수정) |
| 프로세스 생성률 | X | X | X | 어려움 |
| 모듈 로드율 | X | X | X | 어려움 |

> 레지스트리를 "쉬움"으로 재분류함에 따라 "중간" 범주가 현재 특징 후보 내에서는 비어 있을 수 있다. Phase 2에서 실제 특징이 확정되면 재검토한다.

- **유효성 기준**: 교란 후에도 악성 행위의 최소 기능(예: C2 통신 완전 차단이 아닌 지연)이 유지되는 범위 내의 조작만 유효. 이 범위를 벗어나면 6.1절 "무효 변형"으로 처리.

---

## 7. 리소스 벤치마크 조건

- **측정 항목 분리**: (1) 전처리·특징집계 비용, (2) 모델 추론만의 비용 — 이 둘을 분리하여 보고한다.
- **"events per batch"의 정의**: 원시 이벤트가 아니라 **집계 완료된 특징 벡터(샘플)의 배치 크기**를 의미한다. 이벤트 volume(10/100/1,000 events/min)은 원시 이벤트 발생률을, 배치 크기는 그로부터 집계된 특징 벡터 수를 각각 별도로 명시한다.
- **메모리 측정**: `tracemalloc`(Python 할당 메모리, 참고용)과 `psutil.Process().memory_info().rss`(실제 프로세스 물리 메모리, 주 지표)를 **함께** 보고하고 각각의 의미를 명확히 구분해서 서술한다.
- **측정 조건 고정**: 워밍업 1회 실행 후 측정 시작, 단일 스레드 고정, 모델은 사전 메모리 적재된 상태에서 측정 시작.
- **이벤트 볼륨 단계**: 10 / 100 / 1,000 events/min (3단계)
- **반복 횟수**: 각 조건당 20회, 평균±표준편차 보고
- **하드웨어 사양**: 실행 환경의 CPU/메모리 사양을 논문에 명시. 사용자 로컬 환경과 측정 환경이 상이하면 절대값 대신 베이스라인 대비 상대 비율 중심으로 보고(10장 참고).

---

## 8. 통계 처리

- **주평가지표(primary endpoint) 사전 지정**: 탐지 성능은 F1-score를 주지표로 지정한다 (정밀도-재현율 균형, 클래스 불균형 상황에서 정확도보다 안정적). 분류 임계값은 0.5 고정을 기본으로 하되, Phase 3에서 클래스 불균형이 심하면 Validation 기준 Youden's J로 재조정하고 그 사유를 기록한다.
- **모델/축 간 다중 비교**: 4개 모델 × 여러 지표를 비교하므로, 유의성 검정 시 Bonferroni 또는 Benjamini-Hochberg 보정을 적용한다.
- **"차이 없음" 판정 기준 수정**: v1.1의 "모든 지표 차이 < 2%p → 차이 없음"은 폐기한다. 관측 차이가 작다는 것은 동등성이 입증되었다는 뜻이 아니다. 대신 지표별 신뢰구간이 겹치는지로 판단하고, 필요 시 동등성 검정(equivalence test, TOST 등)을 사용한다. 또한 지연시간·메모리처럼 %p 단위가 성립하지 않는 지표에는 상대 비율(%) 또는 절대 차이+신뢰구간을 사용한다.
- **소표본 처리 방식 수정**: v1.1의 "소표본이면 신뢰구간 완화 표기"는 폐기한다. 표본이 작다고 통계적 엄격성 기준 자체를 낮추지 않으며, 대신 결과를 "탐색적 결과(exploratory finding)"로 명시적으로 라벨링하되 신뢰구간 계산 방식은 동일하게 엄격히 적용한다.
- **부트스트랩**: 재표집 단위는 개별 스냅샷이 아니라 **독립 단위(바이너리/실행 그룹)**로 한다. 반복 횟수 2,000회, 95% 신뢰구간(percentile method) 기본 사용.

---

## 9. Phase 6 Test Set 및 MIA 평가셋 보호 원칙

- **탐지 성능 관련**: Phase 4(진단)와 Phase 6 개선안 선택 근거는 Validation으로만 판단. Phase 6 최종 개선 효과는 Test로만 평가. Test는 개선안 선택에 사용하지 않는다.
- **MIA 관련(추가)**: MIA는 정의상 Test를 사용하지 않는다 (5.1절 참고, member=Train/non-member=Validation). 따라서 "Phase 6 최종 효과는 Test로만 평가"라는 원칙은 **탐지 성능 축에만 적용**되며, MIA 개선 효과 검증은 Train/Validation 재구성(단, 동일한 분할 규칙 적용)으로 별도 수행한다.

---

## 10. H5 선택적 개선 의사결정 규칙 (신설)

### 10.1 진단축별 개선 후보 및 조건

| 진단 축 | 개선 후보 | 적용 조건 (Validation 기준) |
|---|---|---|
| 프라이버시(MIA) | 정규화 강화 + 출력 이산화 | MIA AUC > 0.6 |
| 강건성(교란 민감도) | 적대적 학습 (6장 교란 샘플 활용) | 조작 20% 기준 flip rate > 30% |
| 리소스 효율성 | 모델 경량화 (트리 수/은닉층 축소) | 샘플당 추론 지연시간이 Validation 내 최대 허용치(사전 설정: 1ms) 초과 |

### 10.2 예산 및 판단 기준
- **탐색 예산**: 각 개선 후보당 하이퍼파라미터 조합 최대 10개까지 시도 (4장 사전 정의 범위 내)
- **개선 성공 판단 주지표**: 해당 축의 핵심 지표가 Validation에서 **20% 이상 상대적으로 개선**되면 "성공"
- **허용 한도**: 다른 축의 성능 저하가 다음을 초과하면 해당 개선안은 기각: 탐지 F1 저하 5%p 초과, 오탐률 상승 10%p 초과, 지연시간 50% 초과 증가
- **복수 후보 충족 시 타이브레이킹 순서**: (1) 목표축 개선폭이 큰 순 → (2) 부작용(다른 축 저하)이 적은 순 → (3) 구현 복잡도가 낮은 순
- **개선 실패(모든 후보가 허용 한도 초과) 시**: 개선을 적용하지 않고, "해당 취약점에 대해 시도한 개선안이 트레이드오프 기준을 충족하지 못했다"고 명시적으로 보고 (실패도 결과로 기록, 성공 사례만 선별 보고하지 않음)

### 10.3 비교군 설계 (H5 검증 핵심)
Phase 6에서는 아래 3개 조건을 Test에서 비교한다.
1. **무개선 기준선** (베이스라인 모델)
2. **선택적 개선** (10.1~10.2 규칙에 따라 취약 축에만 적용)
3. **일괄개선 비교군** (진단 결과와 무관하게 3개 개선안 모두 적용)

→ H5가 지지되려면, 선택적 개선이 일괄개선 대비 **목표축 개선은 동등 이상이면서 부작용은 더 적어야** 한다.

---

## 11. DMBD 교차검증 설계

- **독립 재현 원칙 유지**: BCCC에서 학습한 모델을 DMBD에 직접 적용(전이)하지 않는다. 동일한 특징 변환·정규화·진단 절차를 DMBD에 독립적으로 적용하여 별도로 학습·진단한다 (v1.1과 동일, 변경 없음).
- **공식 분할 보존**: DMBD가 제공하는 공식 train/test 분할(2024년 신종 악성코드를 test에만 포함하는 시간적 일반화 설계)을 **그대로 보존**한다. 자체 그룹 기반 재분할로 덮어쓰지 않는다. 단, 본 연구의 Validation 확보를 위해 DMBD의 공식 train 부분을 다시 Train/Validation으로 재분할하며, 이때는 2.1절의 그룹 기반 원칙을 적용한다.
- **재현 성공 판정 기준**: 1절 H4 참고.

---

## 12. 중단·대체·제외 기준

| 조건 | 처리 방식 |
|---|---|
| 특정 특징이 두 데이터셋에서 공통 정의되지 않거나 결측률 30% 이상, 또는 측정 의미가 근본적으로 다름 | 3장 규칙에 따라 공통 특징에서 제외하거나 데이터셋별 개별 특징으로 유지 |
| 교란된 표본이 6.2절 유효성 기준(최소 기능 유지)을 벗어남 | 6.1절에 따라 "무효 변형"으로 집계, 성공률 계산에서 제외 |
| B·C(BO/스태킹) 모델의 Phase 3~4 결과가 A(RF기본)와 신뢰구간이 겹칠 정도로 유사 | Phase 6 적용은 생략하되 "차이 없음"이 아니라 "본 실험에서 통계적으로 유의미한 차이를 발견하지 못함"으로 정확히 기술 |
| BCCC 다중 스냅샷의 집계 방식(평균/최대/초기 N개)이 성능에 유의미한 차이(F1 신뢰구간 비중첩)를 만듦 | 모든 결과를 집계 방식별로 병기, 대표값 임의 선택 금지 |
| 특정 모델이 수렴하지 않거나 극단적 과적합(Train/Validation F1 차이 > 20%p) | 4장 사전 정의 하이퍼파라미터 범위 내에서만 재조정. 범위 밖 조정 필요시 해당 모델을 그 진단축에서 제외하고 사유 기록 |
| 리소스 측정 환경(하드웨어) 재현 불가 | 절대 수치 대신 베이스라인 대비 상대 비율 중심 보고, 측정 환경 사양 명시 |
| DMBD 공통 특징 변환 후 그룹 수가 30 미만 | "탐색적 결과"로 한정, 8장 통계 기준(신뢰구간 계산 방식)은 동일하게 유지하되 결과 해석에서 신중히 언급 |
| MIA 공격자 모델 성능이 불안정 (AUC가 시드별로 0.1 이상 요동) | 5.2절 우선순위 체인에 따라 2순위로 자동 전환. **축 제외는 하지 않음** (5.2절 폐기 규칙 참고) |

---

## 13. 전체 Phase 흐름 및 산출물 (신설 요약표)

| Phase | 내용 | 주요 산출물 |
|---|---|---|
| 0 | 본 프로토콜 확정 | 실험_프로토콜_확정문서 |
| 1 | BCCC/DMBD 데이터 구조 파악 | 컬럼·라벨분포·결측·고유샘플수·그룹수 조사 결과 |
| 2 | 공통 특징 확정 | Common Behavioral Feature Set v1.0 (3장 문서화 요건 충족) |
| 3 | 4개 모델(A/B/C/D) 구축 | 베이스라인 탐지 성능표 (Train/Val/Test 그룹기반 분할) |
| 4 | 3대 진단 + 특징중요도 | MIA·교란민감도·리소스 결과, Feature Importance vs Manipulability 대조 |
| 5 | DMBD 독립 재현 | Phase 4 동일 절차의 DMBD 결과, H4 재현판정 |
| 6 | 선택적 개선 (10장 규칙) | 무개선/선택적개선/일괄개선 3자 비교, Test 기준 최종 성능 |
| 7 | 논문 작성 | 최종 원고 |

---

## 버전 이력

- v1.0: Phase 1 착수 직전 최초 확정본
- v1.1: 클래스 불균형 처리 우선순위 신설, 중단·대체·제외 기준에 3개 항목 추가
- v1.2 (본 문서): 외부 검토 반영 — (1) 연구 주장 범위를 "에이전트"에서 "탐지 모델"로 한정 (2) 특징별 정의 문서화 요건 신설 (3) 소표본 대체 규칙을 그룹기반 유지로 수정 (4) Test 고정과 시드 반복 규칙 명확화 (5) MIA 축 제외 규칙 폐기 및 보고문구 수정, MIA 평가셋 설계 구체화 (6) "회피 공격"을 "특징 교란 민감도 실험"으로 명칭 수정, 실행기반 검증 부재를 한계로 명시 (7) 모델을 4종(RF기본/RF+BO/RF+BO+스태킹/MLP)으로 세분화하여 H3 튜닝효과·구조효과 분리 (8) H5 의사결정 규칙 신설 및 일괄개선 비교군 추가 (9) DMBD 공식 train/test 분할 보존 결정 (10) 통계 처리 오류 수정("<2%p=차이없음" 폐기, 소표본 CI 완화 폐기, 부트스트랩 단위 수정) (11) 리소스 측정에 psutil RSS 병행, events/batch 정의 명확화 (12) 문서 자체의 버전표기·장번호·특징개수·난이도분류 불일치 수정
