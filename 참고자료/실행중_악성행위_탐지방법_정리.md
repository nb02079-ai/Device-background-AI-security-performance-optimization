# 실행 중 악성 행위 탐지 방법 정리 (PC / 모바일)

**작성일**: 2026-09-15
**목적**: 프로토콜 v1.7 1A절에서 본 연구를 "실행 중 악성 행위 탐지(malicious execution detection)"로 규정함에 따라, 이 영역의 기술적 선택지를 정리한다. 우리가 선택한 방식(집계된 행위 발생률 + 트리 앙상블)이 전체 지형에서 어디에 위치하는지 파악하고, 논문 2장(관련 연구) 서술의 기반으로 삼는다.

---

## 0. 전체 지형: 관측 계층에 따른 분류

실행 중 탐지는 **"무엇을 관측하느냐"**에 따라 갈린다. 계층이 낮을수록 우회가 어렵지만 수집 비용과 노이즈가 크다.

| 관측 계층 | 대표 데이터 | 장점 | 단점 |
|---|---|---|---|
| 시스템콜 / API 호출 | 호출 시퀀스, 인자 | 우회 난이도 높음, 의미 풍부 | 수집 오버헤드 큼, 시퀀스가 매우 길어짐 |
| 프로세스·리소스 행위 | CPU/메모리/네트워크/파일/레지스트리 이벤트 | 수집 경량, OS 표준 도구로 가능 | 정상 행위와 중첩 많음 |
| 메모리 상태 | 메모리 덤프, VAD 영역 | 파일리스 악성코드 포착 가능 | 덤프 비용 큼, 실시간성 낮음 |
| 네트워크 트래픽 | 플로우, 페이로드 | 호스트 밖에서 관측 가능 | 암호화 시 제약, 호스트 내부 행위 못 봄 |

> **우리 연구의 위치**: 2행(프로세스·리소스 행위)을 주로 쓰되, 데이터셋에 따라 3행(메모리 상태)이 일부 섞인다. 1행(시스템콜/API)은 쓰지 않는다 — 이 선택의 근거는 아래 1-3절 참고.

---

## 1. PC(Windows) 환경

### 1-1. 시스템콜 / API 호출 시퀀스 기반 ⭐ 학계 주류

**개념**: 프로그램이 실행 중 호출하는 Windows API나 시스템콜의 **순서(sequence)**를 특징으로 삼는다. 악성코드는 정상 프로그램과 다른 호출 패턴을 보인다는 가정에 기반한다.

**대표 연구 흐름**:
- **Kolosnjaji et al. (2016)** — 시스템콜 시퀀스에 딥러닝을 적용한 초기 연구
- **XiaoFeng et al.** — API 호출 간 의존 관계를 분석하고 양방향 잔차 신경망으로 탐지, 정확도 96.7% 달성
- **DMalNet (Li et al.)** — 하이브리드 특징 인코더와 호출 그래프로 API 호출 시퀀스와 인자에서 의미 특징을 추출, 2만여 정상/1.8만 악성 실행파일에서 정확도 98.43%
- **MalAnalyser (Dabas & Prabha)** — 전체 API 시퀀스에서 빈발 호출을 추출하고, 입자군집최적화·유전알고리즘으로 소수의 핵심 특징만 선별해 미지 악성코드 탐지 성능을 개선
- **NLP 기법 도입 흐름** — MalDetConv(arXiv:2209.03547), EarlyMalDetect(arXiv:2407.13355) 등이 API 시퀀스를 자연어처럼 다뤄 임베딩·트랜스포머를 적용

**주요 데이터셋**:
- **APIMDS** (IEEE DataPort) — 23,080개 악성 샘플의 API 호출 트레이스. Microsoft Detours 기반 후킹으로 수집했고, DLL 인젝션·IAT 후킹·안티디버깅·화면 캡처 같은 실제 실행 행위를 반영하며 26개 기능 그룹으로 분류되어 있다
- **Windows malware execution traces** (2025.12) — 기존 공개 데이터셋이 세부가 부족하거나 family 다양성이 제한적이거나 단순화된 API 시퀀스만 제공한다는 문제의식에서 구축된 최신 데이터셋

**한계**: 악성코드 진화에 따른 성능 저하 문제가 지적된다(arXiv:2408.01661, "Mitigating the Impact of Malware Evolution on API Sequence-based Windows Malware Detector") — 우리 H4(교차 데이터셋 재현)의 문제의식과 연결된다.

### 1-2. 메모리 포렌식 기반

**개념**: 실행 중 메모리 상태를 덤프해 분석한다. 파일리스 악성코드가 디스크에 흔적을 남기지 않는 문제를 정면으로 다루는 접근이다.

- **VADViT** (Dehfouli & Lashkari, 2025) — 우리 주실험 후보 데이터셋(BCCC-MalMem-SnapLog-2025)의 원 논문. VAD 영역을 마르코프·엔트로피·강도 이미지로 변환 후 비전 트랜스포머로 분류
- **VolMemLyzer** — Volatility 기반 메모리 특징 추출기. CIC-MalMem-2022(대체 후보 데이터셋)와 BCCC-Mal-NetMem-2025가 이 도구로 특징을 추출했다
- Sihwail et al. (2019) — 메모리 이미지 아티팩트와 동적 분석을 결합한 접근

**우리 연구와의 관계**: 우리가 쓸 데이터셋이 이 계열에서 생산된 것이다. 다만 우리는 **원본 메모리 이미지가 아니라 거기서 추출된 표(tabular) 특징**을 쓴다.

### 1-3. 프로세스·리소스 행위 기반 ⭐ 우리가 선택한 방식

**개념**: API 호출 하나하나를 추적하는 대신, **일정 시간 창 동안의 행위 발생률**(프로세스 생성 수, 네트워크 연결 수, 파일/레지스트리 이벤트 수 등)을 집계해 특징으로 쓴다.

**장점**:
- 수집이 경량하다. Windows 기본 도구(Sysmon, ETW)로 가능하며 API 후킹 같은 침습적 기법이 불필요하다
- 실시간 상시 구동에 적합하다 — 우리 프로토콜 1A.2절에서 정리한 "수 분 내 판별" 요건과 부합
- 특징 수가 적어 해석 가능성이 높다 (우리 프로토콜 4.1절 RF 채택 근거와 연결)

**단점**:
- 시퀀스 정보가 소실된다. "A 다음에 B를 호출했다"는 순서 정보가 발생률 집계에서는 사라진다
- 정상 행위와의 중첩이 크다

> **우리가 이 방식을 택한 이유와 한계 명시 의무**: API 시퀀스 기반이 학계 주류이고 보고된 정확도도 높지만(96~98%), (1) 두 데이터셋(BCCC/DMBD) 간 공통 특징으로 삼기에는 스키마 차이가 크고, (2) 상시 구동 에이전트의 리소스 제약이라는 본 연구의 문제의식과 맞지 않으며, (3) 우리 프로토콜 3.3절에서 **표준편차·기울기 특징으로 시간적 변화를 일부 복원**하는 보완책을 두었다. 다만 시퀀스 정보 소실은 구조적 한계이므로 논문 한계점에 명시한다.

### 1-4. 앙상블·투표 기반 (모델 수준)

- 다수 모델의 예측을 집계하는 투표 분류기(voting classifier)로 시스템콜 패턴을 분류하는 접근이 보고된다
- **LCCDE** (Yang et al., GLOBECOM 2022) — 클래스별로 가장 성능이 좋은 "리더 모델"을 선택하는 앙상블. IDS-ML 저장소에 포함된 세 노트북 중 하나
- 우리 프로토콜 4.1절 모델 C(스태킹)가 이 계열에 속한다

---

## 2. 모바일(Android) 환경

### 2-1. PC와 근본적으로 다른 제약

| 항목 | PC | 모바일 |
|---|---|---|
| 권한 모델 | 프로세스 단위, 상대적으로 개방 | **앱별 권한 모델**, 샌드박스 격리 강함 |
| 리소스 제약 | 상대적으로 여유 | **배터리·발열이 결정적 제약** |
| 관측 가능성 | 커널 후킹 등 폭넓음 | 루팅 없이는 타 앱 관측이 매우 제한적 |
| 배포 경로 | 자유 설치 | 앱스토어 심사 존재 |

**핵심 문제**: 동적 행위 기반 모니터링이 Android에서 더 강건한 탐지 능력을 제공하지만, **구현이 어렵다**. "상시 실행" 특성상 리소스 소비가 크고, 동적 모니터링·계측 앱이 배터리 구동 기기에 부담을 가중시키기 때문이다. 이 때문에 앱 재패키징·재서명 없이 런타임에 계측하면서 투명성을 유지하는 기법이 연구된다.

> **우리 연구와의 연결**: 이 "상시 구동 에이전트의 리소스 부담" 문제는 모바일에서 훨씬 첨예하지만, **본질은 우리 프로토콜 7장(리소스 벤치마크)의 문제의식과 동일**하다. 논문에서 리소스 축의 중요성을 논할 때 모바일 사례를 방증으로 인용할 수 있다.

### 2-2. 주요 접근

**① 권한(Permission) 기반 — 정적이지만 실무 지배적**
- Android는 앱별로 필요한 자원을 명시하고 사용자가 설치 시 허용/거부하는 권한 모델을 채택한다. 다만 실제 악성코드 확산은 **사용자가 어떤 권한 요청이든 그냥 신뢰하고 설치한다**는 것을 보여준다
- Drebin 악성 5,000 + Androzoo 정상 5,000 샘플 기준, 권한 특징만으로 RandomForest가 TPR 91.6%로 최고 성능을 기록했다는 보고가 있다 (PLoS ONE, 2021)
- **주목**: 여기서도 RandomForest가 최고 성능 — 우리 프로토콜 4.1절 RF 채택의 방증

**② 시스템콜 기반 — 동적 분석의 주류**
- **CrowDroid** — 기기에서 동작하는 클라이언트가 앱의 시스템콜을 수집해 중앙 서버로 전송, 서버가 앱별 행위 모델을 구축 (온디바이스 수집 + 서버 분석의 분업 구조)
- **DroidTrace** — `ptrace`로 선택된 시스템콜을 모니터링하여 파일 접근·네트워크 연결·IPC·권한 상승 등으로 행위를 분류
- **CopperDroid** — 실행된 시스템콜 관찰로 악성 행위를 재구성
- **SysDroid** — 시스템콜 트레이스 기반 동적 ML 분석기

**③ 네트워크 트래픽 기반**
- Arora & Peddoju — 탐지에 필요한 네트워크 특징을 최소화하는 연구
- **Hybroid** — 프로그램 코드와 네트워크 트래픽을 결합한 하이브리드 탐지
- 암호화된 트래픽에서도 소수 샘플로 Android 랜섬웨어를 탐지하는 연구 흐름 존재

**④ 호출 그래프 / 구조 기반**
- **Drebin** (Arp et al., NDSS 2014) — 이 분야의 기준 문헌. 경량하면서 설명 가능한 탐지를 목표로 한 온디바이스 지향 설계
- Gascon et al. — 임베디드 호출 그래프를 이용한 구조적 탐지

### 2-3. 모바일 쪽에서 참고할 만한 방법론적 교훈

- **적대적 공격 대응 연구가 활발하다**: Li et al. (2021) "Robust android malware detection against adversarial example attacks" — 우리 프로토콜 6장(교란 민감도)과 직결
- **회피형 앱 탐지**: Lumus (Afonso et al., 2018) — 분석 환경을 감지하고 악성 행위를 숨기는 앱을 다룬다. 우리 1A.3절(저활동 잠복형 한계)과 같은 문제
- **스냅샷 기반 조기 예측**: 한 연구는 긴 시퀀스를 다루기 위해 관련 특징을 소수 클러스터 클래스로 묶고, **빠른 시퀀스 스냅샷 분석(rapid-sequence-snapshot)**으로 프로세스의 악성 여부를 조기 예측하는 LSTM 모델을 제안했다 — **우리의 스냅샷 집계 방식(프로토콜 3.3절)과 발상이 유사**하다

---

## 3. 우리 연구의 위치 요약

| 차원 | 주류 방식 | 우리 선택 | 근거 |
|---|---|---|---|
| 관측 계층 | API/시스템콜 시퀀스 | 프로세스·리소스 행위 발생률 | 경량 수집, 상시 구동 적합, 두 데이터셋 공통 특징 확보 |
| 시간 정보 | 시퀀스 순서 보존 | 통계 요약(평균·최대·표준편차·기울기) | 시퀀스 소실을 변동성·추세로 부분 보완 (3.3절) |
| 모델 | 딥러닝(LSTM, 트랜스포머) | 트리 앙상블 + MLP 대조 | 해석 가능성, 추론 지연, 업계 관행 (4.1절) |
| 평가 초점 | 탐지 정확도 | **탐지 + 프라이버시 + 강건성 + 리소스** | 본 연구의 차별점 |

**논문 2장(관련 연구) 서술 방향**: 위 표의 1~3행은 "선행 연구는 X를 택했고 우리는 Y를 택했으며 그 이유는 Z"로 서술하되, **4행이 본 연구의 실질적 기여**임을 분명히 한다. 즉 우리는 "더 좋은 탐지 모델"을 주장하는 것이 아니라 "탐지 정확도 중심 평가의 불충분성"을 논증하는 연구다.

---

## 추천 학습 순서

1. **A Family of Droids** (arXiv:1803.03448) — 정적 vs 동적 분석을 비교 정리, 전체 지형 파악에 가장 적합
2. **Feature-Centric Approaches to Android Malware Analysis: A Survey** (arXiv:2509.10709, 2025) — 최신 특징 중심 서베이
3. **Mitigating the Impact of Malware Evolution on API Sequence-based Detector** (arXiv:2408.01661) — 우리 H4와 직결
4. **VADViT** (2025) — 우리 데이터셋 원 논문 (별도 정리 문서 참고)
5. **Drebin** (NDSS 2014) — 경량·설명가능 탐지의 고전
