# 실험 프로토콜 확정 문서 v1.0

**연구 주제**: PC/서버 백그라운드 보안 에이전트형 AI의 보안 성능 진단 및 개선
**문서 목적**: 실험 시작(Phase 1) 전 모든 방법론적 결정을 사전에 고정하여, 결과를 보고 사후적으로 유리한 조건을 선택하는 것을 방지한다.
**상태**: v1.0 (Phase 1 착수 직전 확정본) — 이후 변경 시 반드시 10장(변경 이력)에 기록

---

## 1. 연구 목적 및 가설

**핵심 연구 질문**: 탐지 성능이 높은 백그라운드 보안 에이전트 AI가 실제로도 안전한가?

**H1**: 높은 악성코드 탐지 성능이 프라이버시·강건성·리소스 효율성을 동시에 보장하지 않는다.
**H2**: 공격자가 쉽게 조작 가능한 행위 특징에 대한 높은 의존도는 회피 공격 성공률과 정(+)의 관계를 가진다.
**H3**: 모델 구조(RF/MLP)와 최적화 방식(기본 vs 베이지안 최적화+스태킹)에 따라 프라이버시 유출·회피 저항성 특성이 달라진다.
**H4**: 특정 취약성은 단일 데이터셋(BCCC)의 우연한 현상이 아니라, 독립 데이터셋(DMBD)에서도 일정한 패턴으로 재현된다.
**H5**: 진단 결과에 기반한 선택적 개선은 실제 취약 축의 성능을 개선하면서 불필요한 트레이드오프를 최소화한다.

> 가설의 검증 방향(정비례/반비례/무관계)은 실험 전에 예단하지 않는다. 특히 H1·H3은 "탐지 성능과 프라이버시 유출이 비례한다"는 특정 방향을 가정하지 않고, 실제 관측된 관계를 그대로 보고한다.

---

## 2. 데이터 분할 및 누수 방지

- **분할 단위**: 샘플(악성코드 개체) 단위 분할을 원칙으로 한다. 동일 악성코드 계열(family)의 서로 다른 변종이 train/validation/test에 걸쳐 나뉘는 경우, 이는 데이터 누수로 간주하고 family 단위 분할로 전환한다.
- **분할 비율**: Train 50% / Validation 20% / Test 30%를 기본값으로 한다.
  - Validation: Phase 4(진단) 및 Phase 6 개선안 선택 근거로만 사용
  - Test: Phase 6 최종 개선 효과 검증에만 사용 (Phase 4 진단에는 절대 사용하지 않음)
- **조건부 규칙**: BCCC의 고유 샘플 수가 100 미만으로 확인될 경우, family 단위 분할 대신 **층화(stratified) k-fold 교차검증(k=5)**으로 대체하고, 이 경우 Validation/Test 분리 원칙은 fold 단위로 재정의한다. (Phase 1에서 샘플 수 확인 후 확정)

---

## 3. 공통 특징 확정 규칙

- Phase 2에서 설계한 잠정 특징 5종(프로세스 생성률, 프로세스 지속시간, 네트워크 연결률, 모듈 로드율, 레지스트리/파일 이벤트율)을 기준으로 한다.
- **제외 규칙**: 특정 특징이 두 데이터셋(BCCC, DMBD) 중 한쪽에 대응 컬럼이 없거나, 결측률 30% 이상인 경우 해당 특징은 공통 특징에서 제외한다.
- **정규화 규칙**: 모든 카운트형 특징은 관찰 시간 창(BCCC 30초 간격 vs DMBD 5분 창)으로 나눈 분당 발생률(rate per minute)로 통일한다.
- 최종 채택 여부는 아래 표 형식으로 논문 방법론(3장)에 명시한다.

| Feature | BCCC 대응 | DMBD 대응 | 결측률 기준 통과 | 최종 채택 |
|---|---|---|---|---|
| Process creation/min | 확인 필요 | O | - | - |
| Process lifetime | 확인 필요 | O | - | - |
| Network connection/min | 확인 필요 | O | - | - |
| Module load/min | 확인 필요 | O | - | - |
| Registry event/min | 확인 필요 ⚠️ | O | - | - |
| File event/min | 확인 필요 ⚠️ | O | - | - |

---

## 4. 모델 및 하이퍼파라미터 규칙

- **모델 3종과 역할**:
  1. RandomForest — 주 배포 후보 (해석 가능성·실시간성·업계 관행 근거)
  2. MLP — 모델 구조 대조군
  3. 베이지안 최적화 + 스태킹 트리 앙상블 — 튜닝 방법론 대조군 (IDS-ML 파이프라인 참고 재구현)
- **실험 범위 차등**: RF·MLP는 Phase 3~6 전체 파이프라인 수행. 최적화+스태킹 모델은 Phase 3~4(베이스라인 성능 + 3대 진단)까지만 수행하며, Phase 6 적용 여부는 Phase 4 결과에 따라 별도 판단한다.
- **하이퍼파라미터 탐색 범위**: 사전에 고정한다 (Phase 1 데이터 확인 후 구체적 수치 확정, 탐색 후 결과를 보고 범위를 넓히지 않는다).
- **랜덤 시드**: 모든 모델 학습·데이터 분할에 seed=42를 기본값으로 사용하고, 통계적 안정성 확인을 위해 seed 5종(42, 7, 123, 2024, 777)으로 반복 후 평균±표준편차를 보고한다.

### 4.1 클래스 불균형 처리 우선순위

Phase 1에서 실제 정상/악성 비율을 확인한 뒤, 불균형이 확인되면(소수 클래스 비율 40% 미만 기준) 아래 우선순위를 순서대로 적용한다.

| 순위 | 방법 | 전환 조건 |
|---|---|---|
| 1순위 | Class Weight 조정 (`class_weight='balanced'` 등) | 기본 적용 |
| 2순위 | Under-sampling (다수 클래스 축소) | 1순위 적용 후에도 소수 클래스 재현율이 0.5 미만으로 유지될 경우 |
| 3순위 | Over-sampling (SMOTE 등) | 1·2순위 모두 부적절할 정도로 소수 클래스 표본이 극히 적은 경우(예: 30개 미만) |

> 어떤 방법을 적용했는지는 모델별·데이터셋별로 11장 변경 이력 표에 기록한다.

---

## 5. 멤버십 추론 공격(MIA) 프로토콜 및 대체 기준

**우선순위 체인** (조건 충족 시 자동 적용, 결과를 보고 선택하지 않음):

| 순위 | 방법 | 적용 조건 |
|---|---|---|
| 1순위 | Shadow-model 기반 MIA (Shokri et al. 2017) | 클래스별 고유 샘플 수 ≥ 100 |
| 2순위 | Confidence-threshold 기반 MIA (Yeom et al. 2018 간소화 방식) | 1순위 조건 미충족 시 자동 전환 |
| 3순위 | 해당 진단 축 제외, 한계점에 명시 | 2순위도 공격자 모델이 AUC 0.5±0.02 범위에서 벗어나지 않고 불안정할 경우 |

- **평가지표**: Attack Accuracy, Precision, Recall, F1, ROC-AUC (모든 경우 공통 보고)
- **Shadow model 수** (1순위 적용 시): 5개, 각각 원본과 동일 분포에서 재샘플링

---

## 6. 회피 공격 범위·조작량·난이도 기준

**난이도 분류 기준** (아래 3개 기준 중 2개 이상 해당 시 "쉬움"으로 분류):
1. 정상적인 악성 기능 수행에 미치는 영향이 낮음 (기능 유지 가능)
2. 조작에 추가 시스템 권한이 필요하지 않음
3. 반복적으로 조작 가능 (탐지 시점마다 재조정 가능)

| 특징 | 기준1 | 기준2 | 기준3 | 분류 |
|---|---|---|---|---|
| 네트워크 연결률 | O | O | O | 쉬움 |
| 파일 이벤트율 | O | O | O | 쉬움 |
| 레지스트리 이벤트율 | O | X | O | 중간 |
| 프로세스 생성률 | X | X | X | 어려움 |
| 모듈 로드율 | X | X | X | 어려움 |

- **조작 대상**: "쉬움" 분류 특징을 우선 대상으로 하고, "중간"은 보조 실험으로 포함. "어려움"은 조작 대상에서 제외.
- **조작량 측정**: 이진 성공/실패 대신 조작 비율(5%/10%/15%/20%/30% 감소)별 탐지 확률 변화를 측정하여 dose-response 형태로 보고한다.
- **유효성 기준**: 조작 후에도 악성 행위의 최소 기능(예: C2 통신 완전 차단이 아닌 지연)이 유지되는 범위 내의 조작만 유효한 공격으로 인정한다. 이 범위를 벗어나는 조작(예: 네트워크 연결을 0으로 만들어 사실상 기능 정지)은 무효 처리한다.

---

## 7. 리소스 벤치마크 조건

- **측정 항목**: 샘플당 평균 추론 지연시간(ms), 피크 메모리(KB), 이벤트 볼륨별 배치 처리 시간
- **이벤트 볼륨 단계**: 10 / 100 / 1,000 events per batch (3단계로 한정, 지속적 실시간 스트림 시뮬레이션은 범위 밖으로 명시)
- **반복 횟수**: 각 조건당 20회 반복 후 평균±표준편차 보고
- **측정 도구**: `time.perf_counter()`(지연시간), `tracemalloc`(메모리)
- **하드웨어 조건**: 실행 환경의 CPU/메모리 사양을 논문에 명시 (재현성 확보)

---

## 8. Random Seed·반복·통계 처리

- 모든 확률적 요소(데이터 분할, 모델 초기화, 회피 공격 탐색)에 대해 4장에 정의된 5개 시드로 반복
- 평균±표준편차로 보고, 개선 전후 비교 시 95% 신뢰구간 표기
- 표본이 작아 정규성이 담보되지 않는 경우, 평균 비교 대신 부트스트랩 신뢰구간 사용 (조건: 클래스별 샘플 수 < 100)

---

## 9. Phase 6 Test Set 보호 원칙

- Phase 4(진단)와 Phase 6 개선안 선택 근거는 **Validation set**으로만 판단한다.
- Phase 6 최종 개선 효과 보고는 **Test set**(2장에서 분리, 어떤 이전 단계에서도 열람하지 않은 데이터)으로만 수행한다.
- Test set은 프로토콜 확정 시점에 분할·해시값 기록하여 이후 어떤 이유로도 재구성하지 않는다.

---

## 10. 중단·대체·제외 기준

| 조건 | 처리 방식 |
|---|---|
| MIA 공격자 모델이 안정적으로 학습되지 않음 (AUC가 시드별로 0.1 이상 요동) | 5장 우선순위 체인에 따라 다음 순위 방법으로 자동 전환 |
| 특정 특징이 두 데이터셋에서 공통 정의되지 않거나 결측률 30% 이상 | 3장 규칙에 따라 공통 특징에서 제외 |
| 특정 회피 조작이 악성 행위의 최소 기능을 유지하지 못함 | 6장 유효성 기준에 따라 해당 perturbation을 유효 공격으로 인정하지 않음 |
| 최적화+스태킹 모델의 Phase 3~4 결과가 RF/MLP와 실질적으로 동일 (모든 지표 차이 < 2%p) | Phase 6 적용 생략, "튜닝 방법론에 따른 차이 없음"으로 보고 |
| BCCC 다중 스냅샷의 집계 방식(평균/최대/초기 N개)이 성능에 유의미한 차이(F1 차이 > 5%p)를 만듦 | 모든 결과를 집계 방식별로 병기하고, 하나를 "대표값"으로 임의로 선택하지 않음 |
| 특정 모델이 수렴하지 않거나 극단적 과적합(Train/Validation F1 차이 > 20%p)을 보임 | 4장에서 사전 정의한 하이퍼파라미터 탐색 범위 내에서만 재조정 시도. 범위 밖 조정으로만 해결 가능하면 해당 모델을 그 진단 축에서 제외하고 사유 기록 |
| 사용자 로컬 환경과 리소스 측정 환경(하드웨어)이 상이하여 절대 수치 재현이 어려움 | 절대 지연시간·메모리 수치 대신, 동일 환경 내 베이스라인 대비 상대 비율(예: "베이스라인 대비 82% 단축")을 중심으로 보고. 원 측정 환경 사양은 반드시 명시 |
| DMBD로 공통 특징 변환 후 클래스별 유효 표본이 소규모(예: 30개 미만)로 확인됨 | 해당 결과는 "참고 결과(reference finding)"로만 한정하고, 8장 통계 처리 기준(신뢰구간)을 완화 표기하며 한계점에 명시. 정식 검증 결과로 주장하지 않음 |

---

## 11. 변경 이력 및 예외 기록 규칙

프로토콜 확정 이후 발생하는 모든 예외 상황은 아래 표 형식으로 기록한다.

| 발생 조건 | 원래 계획 | 실제 처리 | 변경 이유 | 영향 범위 | 재현 정보 (seed/버전/파라미터) |
|---|---|---|---|---|---|
| (예시) | | | | | |

> 이 표는 Phase 1 착수 이후 실시간으로 갱신하며, 논문 작성 시 방법론(3~4장) 또는 부록에 요약 반영한다.

---

## 버전 이력

- v1.0: Phase 1 착수 직전 최초 확정본
- v1.1 (본 문서): 클래스 불균형 처리 우선순위(4.1절) 신설, 중단·대체·제외 기준(10장)에 모델 수렴 실패·리소스 측정환경 재현 불가·DMBD 소표본 3개 항목 추가. IDS-ML/최적화+스태킹 모델은 3번째 실험 모델로 유지하기로 재확인(변경 없음).
